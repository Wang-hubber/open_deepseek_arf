# 贡献者须知

ARF 是一个约定大于配置的智能体框架。理解四种实体类型（model、tool、skill、hook）以及基于文件系统的资源模型，是参与贡献的关键。

---

## 1. 开发环境搭建

```bash
git clone git@gitee.com:dalaydata/open_deepseek_arf.git
cd open_deepseek_arf
pip install -e .
cd frontend && npm install && cd ..
```

### 启动开发环境

```bash
# 方式一：一键启动（推荐）
arf init dev_workspace
arf start --workspace dev_workspace

# 方式二：分别启动
arf web --workspace dev_workspace &
cd frontend && npm run dev
```

浏览器打开 `http://localhost:5173`，前端开发服务器会自动代理 API 请求到后端的 `localhost:8000`。

### 验证资源完整性

```bash
arf validate                  # 检查所有资源结构
arf list tools                # 列出所有已注册工具
arf list skills               # 列出所有已注册技能
arf list models               # 列出所有已注册模型
```

---

## 2. 代码架构速览

```
src/arf/
├── cli.py                    # CLI 入口（613 行），所有 arf 命令的定义
├── agent/
│   ├── __init__.py           # ARFAgent 核心编排层，提示词管线，内核工具定义
│   └── project.py            # 工作区脚手架（create_workspace）
├── engine/
│   ├── graph.py              # GraphEngine — LangGraph StateGraph 封装
│   ├── state.py              # AgentState TypedDict（消息、token 用量、tool 事件）
│   ├── nodes.py              # 图节点：classify、call_model、execute_tools、recovery
│   ├── router.py             # 条件边：decide_entry、route_after_model 等
│   ├── classifier.py         # 任务复杂度分类器（三级模型路由）
│   ├── tracing.py            # DevTracer — 基于日志的追踪
│   └── event_mapper.py       # LangGraph v2 streaming → ARF 事件映射
├── resources/
│   ├── __init__.py           # 数据类定义（Model、Tool、Skill 基类）
│   ├── manager.py            # ResourceRegistry — 双源文件系统加载器
│   ├── model_adapter.py      # OpenAI 兼容 API 封装（重试、流式、thinking）
│   └── system/
│       ├── models/           # 内置模型配置（9 种类型）
│       ├── tools/            # 内置工具（17 个，11 个完整实现）
│       └── skills/           # 内置技能（14 个）
├── server/
│   ├── __init__.py           # ARFServer — FastAPI 应用创建
│   ├── routes.py             # 全部 REST API 端点（935 行）
│   ├── session_manager.py    # SessionManager — 中心状态管理器
│   ├── ws.py                 # WebSocket 处理器（会话生命周期）
│   ├── database.py           # SQLite 数据库（6 张表）
│   ├── hook_runner.py        # 子进程 Hook 执行引擎
│   ├── fast_model.py         # 快速模型调用辅助
│   └── static/               # 前端构建产物目录（npm run build 后填充）
└── hooks/                    # 内置 Hook 脚本（4 个）

frontend/src/                 # Vue 3 + TypeScript 前端（开发环境）
├── main.ts
├── App.vue
├── router/index.ts
├── stores/                   # Pinia Store（app、sessions、chat）
├── views/                    # 页面视图（8 个）
├── components/               # 组件（13 个）
├── composables/              # 组合式函数（8 个）
└── types/                    # TypeScript 类型定义
```

### 关键设计决策

- **ARFAgent 不直接调用模型** — 它通过依赖注入将 callable 传递给 GraphEngine，GraphEngine 对具体资源一无所知。
- **工具函数在进程内执行** — `function.py` 被 `importlib` 动态加载，函数签名必须与 `tool.yaml` 中定义的 Schema 匹配。
- **Hook 在子进程中执行** — 通过 `HookRunner` 以子进程方式运行，通过环境变量（小负载）和 stdin JSON（大负载）传递上下文。退出码约定：0 = 继续，1 = 阻止，2 = 注入。
- **热重载基于文件监听** — `watchfiles.awatch()` 监听 `tools/`、`skills/`、`models/` 目录，1 秒去抖后触发 `registry.reload_user()`。

---

## 3. 如何添加一个新工具

### 3.1 目录结构

一个完整的工具需要以下文件：

```
tools/<tool_name>/
├── tool.yaml           # 工具定义 + JSON Schema
├── config_default.yaml # 元数据、标签、触发模式
└── function.py         # 实现代码
```

### 3.2 tool.yaml 模板

```yaml
name: my_tool
description: >
  一句话描述工具的功能。这行文本会出现在系统提示词中，
  agent 据此判断何时调用此工具。

tool_definition:
  type: function
  function:
    name: my_tool
    description: >
      详细的工具描述，OpenAI function calling 格式。
    parameters:
      type: object
      properties:
        param1:
          type: string
          description: 参数1的描述
        param2:
          type: integer
          description: 参数2的描述
      required:
        - param1
```

### 3.3 function.py 模板

```python
"""my_tool — 工具功能简述"""

import logging
logger = logging.getLogger("arf.tools.my_tool")


async def execute(param1: str, param2: int = 10, ctx: dict | None = None):
    """工具的主执行函数。

    Args:
        param1: 参数1（与 tool.yaml 中的 Schema 对应）
        param2: 参数2（默认值 10）
        ctx: 执行上下文（workspace_dir, registry 等，由框架注入）

    Returns:
        dict: {"status": "success", "data": ...}  或
              {"status": "error", "message": "..."}
    """
    # 实现逻辑
    result = f"处理完成: {param1} x {param2}"

    return {
        "status": "success",
        "data": {"result": result}
    }
```

关键约定：
- 函数名必须是 `execute`
- 参数名必须与 `tool.yaml` 中定义的 `properties` 一一对应
- 返回值必须是 `dict`，包含 `status` 字段（`"success"` 或 `"error"`）
- 如需访问工作区路径，接收 `ctx: dict | None = None` 参数
- 使用 `async def` 支持异步 I/O；纯 CPU 计算可用同步函数
- 日志使用 `logging.getLogger("arf.tools.<name>")`

### 3.4 config_default.yaml 模板

```yaml
# 工具的元数据配置（可选）
hints:
  - "触发此工具的典型用户输入模式1"
  - "触发此工具的典型用户输入模式2"
tags:
  - "类别标签1"
  - "类别标签2"
```

### 3.5 放置在正确位置

**系统工具（随框架发布）：**

```bash
mkdir src/arf/resources/system/tools/my_tool
# 将 tool.yaml、config_default.yaml、function.py 放入该目录
arf validate    # 验证结构正确
arf list tools  # 确认已注册，标签应为 [sys]
```

**用户工具（仅当前工作区）：**

```bash
mkdir my_workspace/tools/my_tool
# 将 tool.yaml、config_default.yaml、function.py 放入该目录
# 无需手动注册 —— 热重载会自动发现
arf list tools  # 确认已注册，标签应为 [usr]
```

### 3.6 参考示例

最完整的参考实现：`src/arf/resources/system/tools/web_fetch/`
- `tool.yaml` — 标准的 OpenAI function calling 格式
- `function.py` — 带重试的 HTTP 请求 + HTML → Markdown 转换

简单实现示例：`src/arf/resources/system/tools/file_reader/`

---

## 4. 如何添加一个新技能

### 4.1 目录结构

```
skills/<skill_name>/
├── skill.yaml           # 技能定义（必需）
└── config_default.yaml  # 元数据（可选）
```

### 4.2 skill.yaml 模板

```yaml
name: my_skill
description: >
  一句话描述技能的用途。agent 读取此描述来判断何时加载此技能。

system_prompt: |
  你是一个专注于某领域的助手。
  当用户提出相关需求时，你应该：
  1. 首先询问用户的具体需求
  2. 使用 file_reader 查看相关文件
  3. 根据分析结果给出建议

  注意：始终先阅读再行动，不要假设文件内容。

recommended_tools:
  - file_reader
  - web_fetch
```

关键约定：
- `system_prompt` 是核心——它被注入到 agent 的系统提示词中，覆盖或增强默认行为
- `recommended_tools` 列出此技能可能需要的工具名称（按名称引用，不是按路径）
- 技能不包含可执行代码——它纯粹通过提示词工程来引导 agent 的行为

### 4.3 config_default.yaml

```yaml
hints:
  - "触发此技能的典型用户输入"
tags:
  - "类别标签"
trigger_on:
  - "关键词匹配模式"
```

### 4.4 参考示例

- `src/arf/resources/system/skills/error_handler/` — 错误恢复流程的标准实现
- `src/arf/resources/system/skills/resource_scaffold/` — 资源脚手架生成的实现

---

## 5. 如何添加一个新模型类型

### 5.1 目录结构

```
models/<model_type>/
└── config_default.yaml
```

### 5.2 config_default.yaml 模板

```yaml
name: my_model_type
model_type: my_model_type
description: "模型类型的简短描述"
config:
  base_url: "https://api.example.com"
  api_key: ""
  model_name: "example-model-v1"
  temperature: 0.7
  max_tokens: 8192
  thinking_enabled: false
  reasoning_effort: "medium"
```

### 5.3 何时需要扩展 ModelAdapter

如果新模型类型完全兼容 OpenAI API（即支持 `/v1/chat/completions` 端点），你只需要提供 `config_default.yaml` 即可。框架的 `model_adapter.py` 使用 OpenAI 兼容的 API 调用。

如果新模型需要特殊的 API 格式（非 OpenAI 兼容），你需要扩展 `src/arf/resources/model_adapter.py` 中的 `ModelAdapter` 类。

### 5.4 参考示例

- `src/arf/resources/system/models/deep_thinking/config_default.yaml` — 完整推理模型配置
- `src/arf/resources/system/models/embedding/config_default.yaml` — 多模态模型骨架

---

## 6. 如何添加一个新 Hook

### 6.1 Hook 的工作原理

Hook 是外部脚本或程序，由 `HookRunner` 在特定事件发生时以子进程方式调用。

**事件类型：**
- `PreModelCall` — 模型调用前
- `PostModelCall` — 模型调用后
- `PreToolUse` — 工具执行前
- `PostToolUse` — 工具执行后
- `SessionStart` — 会话开始时
- `SessionEnd` — 会话结束时

**Hook 约定：**
- 上下文通过环境变量传递：`ARF_HOOK_EVENT`、`ARF_HOOK_WORKSPACE`、`ARF_HOOK_SESSION_ID`
- 大负载（如消息历史）通过 stdin JSON 传递
- 结果通过 stdout JSON 返回
- 退出码约定：
  - `0` — 继续正常流程
  - `1` — 阻止操作（阻止模型调用或工具执行）
  - `2` — 注入内容（修改或增强消息）

### 6.2 Hook 脚本模板

```python
#!/usr/bin/env python3
"""my_hook — 自定义 Hook 脚本"""

import json
import os
import sys


def main():
    event = os.environ.get("ARF_HOOK_EVENT", "")
    workspace = os.environ.get("ARF_HOOK_WORKSPACE", "")
    session_id = os.environ.get("ARF_HOOK_SESSION_ID", "")

    # 从 stdin 读取大负载
    try:
        payload = json.load(sys.stdin)
    except json.JSONDecodeError:
        payload = {}

    # 处理逻辑
    if event == "PostModelCall":
        # 例如：记录模型调用统计
        usage = payload.get("usage", {})
        result = {
            "action": "continue",
            "data": {"logged_tokens": usage.get("total_tokens", 0)}
        }
    else:
        result = {"action": "continue"}

    # 输出结果到 stdout
    json.dump(result, sys.stdout)
    sys.exit(0)  # 0 = 继续


if __name__ == "__main__":
    main()
```

### 6.3 注册 Hook

Hook 在 `.hooks.json` 中注册（位于工作区根目录）：

```json
{
  "hooks": [
    {
      "name": "my_hook",
      "command": "python3",
      "args": ["/path/to/my_hook.py"],
      "events": ["PostModelCall", "SessionEnd"],
      "timeout": 30,
      "enabled": true
    }
  ]
}
```

### 6.4 参考示例

- `src/arf/hooks/system_log.py` — 最简 Hook，记录所有事件到日志文件
- `src/arf/hooks/memory_extractor.py` — SessionEnd Hook，从对话中提取长期记忆
- `src/arf/hooks/session_archiver.py` — SessionEnd Hook，将会话归档为 JSON
- `src/arf/server/hook_runner.py` — Hook 执行引擎的完整实现

---

## 7. 约定大于配置的设计哲学

ARF 的核心理念是：**规则是固定的，不需要配置的就是最好的配置。**

### 路径约定

| 实体 | 路径约定 | 发现方式 |
|------|---------|---------|
| 模型 | `models/<name>/config.yaml` | 自动扫描 |
| 工具 | `tools/<name>/tool.yaml` + `function.py` | 自动扫描 |
| 技能 | `skills/<name>/skill.yaml` | 自动扫描 |
| 工作区配置 | `<root>/arf_agent.yaml` | 向上遍历目录树查找 |

### 实现的约定

- 工具函数名必须是 `execute`
- 工具返回值必须是 `dict`，包含 `status` 字段
- 技能必须包含非空的 `system_prompt`
- Hook 必须遵循退出码约定（0/1/2）
- 使用 `async def` 处理 I/O，同步函数处理 CPU 计算
- 使用 `pathlib`，不使用 `os.path`
- 日志使用 `logging.getLogger("arf.<module>")`

### 何时打破约定

约定可以被覆盖，但应该有充分理由：
- 用户工作区中的同名资源自动覆盖系统资源
- `arf clone` 是推荐的覆盖方式（保留系统资源作为参考）
- 模型配置合并而非覆盖（用户值 > 系统默认值）

---

## 8. 编码规范

### Python

- Python 3.10+ 语法，充分利用类型注解
- 行宽上限 120 字符
- 工具函数不引入外部依赖——使用框架已有的依赖（标准库 + `openai`、`PyYAML` 等）
- `tool.yaml` 中的 `tool_definition.function.parameters` 必须是完整的 JSON Schema
- `skill.yaml` 中的 `system_prompt` 必须非空且有实际内容
- 异步函数用于 I/O 操作，同步函数用于 CPU 密集计算
- 错误信息使用英文，面向开发者和 agent

### 前端

- Vue 3 Composition API + `<script setup>` 语法
- TypeScript 严格模式
- 使用 Pinia 管理全局状态
- 使用 Vite 的路径别名：`@/` 映射到 `src/`

### 通用

- 新增功能优先考虑能否用 skill（提示词工程）实现，而非增加工具代码
- 如果新增特性需要第五种实体类型（model/skill/tool/hook 之外），重新审视设计
- 资源名称使用 `snake_case`，目录名与资源名一致

---

## 9. Pull Request 工作流

1. **创建分支：** `git checkout -b feature/my-feature`
2. **实现功能：** 添加工具/技能/模型/Hook
3. **验证完整性：**
   ```bash
   arf validate            # 检查资源结构
   arf list tools          # 确认工具注册正确
   arf list skills         # 确认技能注册正确
   ```
4. **前端改动（如有）：**
   ```bash
   cd frontend && npm run build   # 确认 TypeScript 编译和 Vite 构建通过
   ```
5. **编写测试：** `pytest tests/`
6. **提交：** 使用描述性的提交信息
7. **推送并创建 PR：** 针对 `main` 分支
8. **CI 验证：** PR 会触发 Docker 构建（GitHub Actions）和阿里云 ACR 推送（Gitee CI）

---

## 10. 测试

### 验证命令

```bash
# 资源完整性检查
arf validate

# 资源注册确认
arf list tools
arf list skills
arf list models

# CLI 命令测试
arf init test_workspace
arf web --workspace test_workspace --host 127.0.0.1 --port 9999 &
# 等待启动后：
curl http://127.0.0.1:9999/api/config/status

# 清理
rm -rf test_workspace
```

### 手动测试

```bash
# 启动开发环境
arf start --workspace dev_workspace

# 在浏览器中测试：
# 1. 模型连接配置和测试
# 2. 创建新会话并发送消息
# 3. 验证工具调用的展示
# 4. 验证流式 SSE 输出
# 5. 检查资源面板的正确显示
# 6. 测试文件上传功能
# 7. 验证会话归档功能
```

### 代码审查要点

审查时重点关注：
- [ ] `tool.yaml` 中的 JSON Schema 是否完整且正确
- [ ] `function.py` 的 `execute` 函数签名是否与 Schema 匹配
- [ ] 返回值是否包含 `status` 字段
- [ ] 是否引入了新的外部依赖
- [ ] 资源名称是否使用 `snake_case`
- [ ] `skill.yaml` 的 `system_prompt` 是否清晰有用
- [ ] 如果是系统资源，是否放置在 `src/arf/resources/system/` 下
