# App 开发者须知

ARF 提供 Agent 运行所需的全部基础设施——资源发现与热加载、内存管理、模型调度、安全沙箱、可观测性。你只需要关注四件事：**用哪些模型、给什么工具、怎么组织技能、何时触发 Hook**。四种实体按约定的目录结构放置，框架自动发现。

---

## 快速开始

```bash
git clone git@gitee.com:dalaydata/open_deepseek_arf.git
cd open_deepseek_arf
pip install -e .
cd app/arf_default_assistant
python test_setup.py   # 验证环境
python cli.py start    # 启动服务
```

浏览器打开 `http://127.0.0.1:8000`。

---

## 最小可运行的 App

新建一个目录，放一个 `agent.yaml`：

```yaml
name: my_agent
description: 我的第一个 ARF Agent

models:
  - name: default
    model: deepseek-v4-flash
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 800000
```

然后 `export DEEPSEEK_API_KEY=sk-xxx`，写一个 `server.py`（见下文），启动即可对话。框架自动完成模型适配器注入、EventBus 创建、状态管理、上下文压缩——app 层不需要管这些。

---

## 目录结构

```
my_app/
├── agent.yaml      # Agent 配置（模型声明、可选覆盖）
├── server.py       # FastAPI 入口（如果不想写，参考 arf_default_assistant/server.py）
├── tools/          # 自定义工具（每个工具一个子目录）
│   └── my_tool/
│       ├── tool.yaml
│       └── function.py
├── models/         # 模型声明文件（可选，也可在 agent.yaml 内联）
│   └── my_model.yaml
├── skills/         # 技能定义 YAML
│   └── my_skill.yaml
├── hooks/          # Hook 脚本
└── memory/         # 运行时数据（框架自动生成）
```

参考实现：`app/arf_default_assistant/`。

---

## 四种实体 → 分章阅读

| 实体 | 文档 | 说明 |
|------|------|------|
| Model | [配置模型 →](docs/app/models.md) | 声明 API 端点、上下文窗口、推理参数 |
| Tool | [编写工具 →](docs/app/tools.md) | `tool.yaml` Schema + `function.py` 实现，框架自动发现 |
| Skill | [技能与流水线 →](docs/app/skills.md) | 工具组合 + 可选 pipeline 依赖声明 |
| Hook | [生命周期钩子 →](docs/app/hooks.md) | 六个事件点、退出码约定、子进程脚本 |

---

## 进阶主题

| 主题 | 文档 | 说明 |
|------|------|------|
| 双 Agent 架构 | [User Agent + Sys Agent →](docs/app/dual-agent.md) | 任务分离、handoff 交接 |
| 高级配置 | [记忆/路由/压缩/权限 →](docs/app/advanced.md) | `advanced:` 段的所有配置项 |
| 定制 server.py | 下文 | 从框架的最小封装到生产级 FastAPI |

---

## 定制 server.py

参考 app 的 `server.py` 自行编写——框架不强制任何 HTTP 层。最简单的 server.py：

```python
from arf.agent.factory import create_agent
from arf.agent.config import AgentConfig
from fastapi import FastAPI
from fastapi.responses import JSONResponse

app = FastAPI()
cfg = AgentConfig.from_yaml("agent.yaml")
agent = create_agent(config=cfg)

@app.post("/api/chat")
async def chat(req: dict):
    result = await agent.chat(req["message"])
    return JSONResponse({"content": result})
```

参考 app 在此基础上增加了 SSE streaming、undo、trace API、配置管理、双 agent 路由等。详见 `app/arf_default_assistant/server.py`。

---

## 框架目录速览

了解框架模块有助于理解运行时行为，通常不需要修改：

```
arf/
├── agent/          # BaseAgent — 组装所有协议实现，自动注入 call_model
├── engine/         # GraphEngine — 主循环：memory→route→compact→call→guard→execute
├── resources/      # 资源系统：三个 Provider + ResourceResolver + FileWatcher
├── memory/         # FileMemoryStore、LLMMemoryWriter、LLMMemoryRetriever
├── compaction/     # SlidingWindowCompactor — token 感知窗口压缩
├── routing/        # TwoTierRouter — 快慢模型调度
├── guardrails/     # PathCheckToolGuard、ToolPermissionChecker
├── hooks/          # SubprocessHookRunner — 六个生命周期事件
├── sandbox/        # PathSandbox — 路径合法性校验
├── observability/  # FileTraceStore、UsageTracker
├── skills/         # SkillPipeline — 工具依赖执行时序
└── core/           # 协议定义、Pydantic 配置模型、事件类型
```

---

## 进一步阅读

| 文档 | 内容 |
|------|------|
| [内存管理](docs/memory-management.md) | 压缩管道、记忆抽取与检索、OS 演进 |
| [模型路由](docs/model-routing.md) | 快慢分流、system model、KV cache |
| [工具沙箱](docs/tool-sandbox.md) | 路径沙箱、权限分级、Hook 退出码 |
| [资源注册与发现](docs/resource-registry.md) | 文件系统真相源、Provider、FileWatcher |
| [并发与死锁](docs/skill-pipeline.md) | Skill Pipeline、工具执行顺序 |
| [外部中断](docs/interrupt.md) | 取消、undo、Hook 消息注入 |
| [Trace 可观测性](docs/trace.md) | 事件系统、瀑布流、UsageTracker |
