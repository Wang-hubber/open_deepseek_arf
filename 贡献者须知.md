# 贡献者须知

ARF 是一个约定大于配置的智能体框架。理解四种实体类型（model、tool、skill、hook）以及它们在文件系统中的约定，是参与贡献的关键。

框架提供默认实现，应用层可直接使用，也可按需覆盖。

---

## 环境搭建

```bash
git clone git@gitee.com:dalaydata/open_deepseek_arf.git
cd open_deepseek_arf
pip install -e .
cd app/web && npm install && cd ../..
```

启动：`arf start`，浏览器打开 `http://localhost:5173`。

---

## 四种实体类型

### Model

模型配置在 `agent.yaml` 中声明。框架默认支持 OpenAI 兼容 API（DeepSeek 等），可扩展。

```yaml
models:
  - name: quick
    model: deepseek-v4-flash
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 800000
    kwargs:
      temperature: 0.7
```

### Tool

工具由两个文件组成，放在 `tools/<name>/` 目录下。框架自动发现。

**tool.yaml** — Schema 定义：

```yaml
name: my_tool
description: 一句话描述工具功能
parameters:
  type: object
  properties:
    path:
      type: string
      description: 文件路径
  required: [path]
execution:
  sandbox: inherit
  timeout: 30s
activation: kernel
```

**function.py** — 实现：

```python
async def execute(path: str) -> dict:
    """参数名和类型必须与 tool.yaml 的 properties 一致"""
    content = open(path).read()
    return {"content": content, "size": len(content)}
```

关键约定：
- 函数名必须是 `execute`，参数与 `tool.yaml` 的 `properties` 对应
- 返回值 `dict`，无固定格式要求
- 使用 `async def`

### Skill

Skill 声明可用工具和可选的执行流水线，放在 `agent.yaml` 的 `skills` 段：

```yaml
skills:
  - name: code_review
    description: Review code changes for correctness
    tools: [file_reader, file_writer]
    activation: discoverable
    pipeline:                # 可选：声明工具执行顺序
      - tool: file_reader
      - tool: file_writer
        depends_on: [file_reader]
```

- `pipeline` 是可选字段，不填则由 LLM 自主决定调用顺序
- 引擎强制执行 pipeline 中的依赖关系

### Hook

Hook 是独立子进程脚本，在 `agent.yaml` 中注册。框架在六个生命周期事件点调用：

| 事件 | 触发时机 |
|------|----------|
| `session_start` | 会话开始 |
| `pre_model_call` | 模型调用前 |
| `post_model_call` | 模型调用后 |
| `pre_tool_exec` | 工具执行前 |
| `post_tool_exec` | 工具执行后 |
| `session_end` | 会话结束 |

退出码约定：`0` = 继续，`1` = 阻断，`2` = 注入消息到对话流。

```yaml
hooks:
  - name: self_evolve
    type: post_tool_exec
    run: ["python", "./hooks/self_evolve.py"]
    timeout: 5s
```

---

## 框架 vs. 应用

| 层级 | 目录 | 说明 |
|------|------|------|
| **框架** | `arf/` | 引擎、协议、资源系统、Trace 等默认实现。应用可覆盖。 |
| **应用** | `app/arf_default_assistant/` | 基于框架构建的参考智能体。`agent.yaml` 声明所有配置。 |
| **用户工作区** | 用户创建的 workspace | `agent.yaml` + 自定义 tools/skills，按名称覆盖框架默认。 |

---

## 关键目录

```
arf/
├── agent/          # BaseAgent — 组装所有协议实现
├── engine/         # GraphEngine — DI 驱动的执行循环
├── memory/         # FileMemoryStore, LLMMemoryWriter, LLMMemoryRetriever
├── compaction/     # SlidingWindowCompactor — Token 感知滑动窗口
├── routing/        # TwoTierRouter — 快慢模型调度
├── guardrails/     # PathCheckToolGuard, ToolPermissionChecker
├── observability/  # FileTraceStore, UsageTracker
├── hooks/          # SubprocessHookRunner
├── skills/         # SkillPipeline — 工具执行时序
├── core/           # 协议、配置 Pydantic 模型、事件类型
├── resources/      # DefaultToolResolver, StaticYamlToolProvider
└── sandbox/        # PathSandbox

app/arf_default_assistant/
├── agent.yaml      # 所有配置入口
├── server.py       # FastAPI 应用
├── tools/          # 应用层工具
├── hooks/          # Hook 脚本
├── memory/         # 运行时数据（memory.json, archive.json, sessions/）
└── workspaces/     # 工具操作的文件空间
```

---

## 覆盖框架默认实现

框架的所有组件通过 `BaseAgent.__init__` 的 `**override_protocols` 参数注入。应用层在创建 Agent 时可以替换任意默认实现：

```python
agent = BaseAgent(config, memory_store=MyCustomStore(), memory_writer=MyCustomWriter())
```

YAML 配置同样可以覆盖默认行为——`agent.yaml` 中的 `advanced` 段控制记忆、路由、压缩、沙箱等子系统的策略和参数。
