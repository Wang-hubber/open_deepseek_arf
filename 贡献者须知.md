# App 开发者须知

ARF 提供 Agent 运行所需的全部基础设施——内存管理、模型调度、安全沙箱、可观测性。你只需要关注四件事：**用哪些模型、给什么工具、怎么组织技能、何时触发 Hook**。四种实体按约定的目录结构放置，框架自动发现。

---

## 环境搭建

```bash
git clone git@gitee.com:dalaydata/open_deepseek_arf.git
cd open_deepseek_arf
pip install -e .
cd app/web && npm install && cd ../..
```

启动：`arf start`，浏览器打开 `http://localhost:5173`。

---

## 创建你的第一个 App

最小可运行的 App 只需要一个 `agent.yaml`：

```yaml
name: my_agent
description: 我的第一个 ARF 智能体

models:
  - name: default
    model: deepseek-v4-flash
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 800000

tools: []
skills: []
```

将这个文件放在你的 workspace 目录，然后用 `arf start` 启动。框架自动加载模型配置，创建 Agent 实例，挂载 FastAPI 服务。

**推荐的目录结构：**

```
my_app/
├── agent.yaml      # 所有配置入口（模型、工具、技能、Hook、高级选项）
├── tools/          # 你编写的工具（每个工具一个子目录）
├── skills/         # 技能定义（可选，也可在 agent.yaml 中内联）
├── hooks/          # Hook 脚本（可选）
├── memory/         # 运行时数据（自动生成：memory.json, sessions/）
└── workspaces/     # 工具操作的文件空间（自动生成）
```

参考实现：`app/arf_default_assistant/`。

---

## 四种实体类型

### Model — 配置模型

在 `agent.yaml` 的 `models` 段声明。框架默认支持所有 OpenAI 兼容 API（DeepSeek、OpenAI、vLLM 等）。

```yaml
models:
  - name: quick
    model: deepseek-v4-flash
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 800000
    kwargs:
      temperature: 0.7

  - name: deep
    model: deepseek-v4-pro
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 1000000
```

`api_key_env` 指定从哪个环境变量读取密钥。配置多个模型时，框架自动启用路由（见 [高级配置](#高级配置)）。

### Tool — 编写工具

工具由两个文件组成，放在 `tools/<name>/` 目录下。框架自动发现，无需手动注册。

**tool.yaml** — Schema 定义：

```yaml
name: my_tool
description: 一句话描述工具功能
parameters:
  type: object
  properties:
    path:
      type: string
      description: 文件路径
  required: [path]
execution:
  sandbox: inherit
  timeout: 30s
activation: kernel          # kernel: 始终激活；discoverable: 按需加载
```

**function.py** — 实现：

```python
async def execute(path: str) -> dict:
    """参数名和类型必须与 tool.yaml 的 properties 一致"""
    content = open(path).read()
    return {"content": content, "size": len(content)}
```

关键约定：
- 函数名必须是 `execute`，参数与 `tool.yaml` 的 `properties` 一一对应
- 返回值 `dict`，无固定格式要求（整个 dict 作为工具结果传给 LLM）
- 使用 `async def`
- `activation: kernel` 的工具始终在 Agent 的工具列表中，`discoverable` 的工具按需通过 `resource_loader` 加载

### Skill — 组织技能

Skill 将多个工具组合为一个可发现的能力，可声明执行流水线和依赖关系。定义在 `agent.yaml` 的 `skills` 段：

```yaml
skills:
  - name: code_review
    description: 审查代码变更的正确性
    tools: [file_reader, file_writer]
    activation: discoverable

  - name: resource_scaffold
    description: 创建新的工具或技能资源
    tools: [file_writer, resource_loader]
    activation: discoverable
    pipeline:                    # 可选：声明工具执行顺序
      - tool: file_writer
      - tool: resource_loader
        depends_on: [file_writer]
```

- `pipeline` 是可选字段，不填则由 LLM 自主决定调用顺序
- 引擎强制执行 pipeline 中的依赖关系——依赖未满足的工具步骤会被硬阻断

### Hook — 挂载钩子

Hook 是独立子进程脚本，在 Agent 的六个生命周期事件点触发。在 `agent.yaml` 的 `hooks` 段注册：

| 事件 | 触发时机 |
|------|----------|
| `session_start` | 会话开始 |
| `pre_model_call` | 模型调用前 |
| `post_model_call` | 模型调用后 |
| `pre_tool_exec` | 工具执行前 |
| `post_tool_exec` | 工具执行后 |
| `session_end` | 会话结束 |

退出码约定：`0` = 继续执行，`1` = 阻断当前操作，`2` = 注入消息到对话流（LLM 下一轮可见）。

```yaml
hooks:
  - name: log_tool_calls
    type: post_tool_exec
    run: ["python", "./hooks/log_tool_calls.py"]
    timeout: 5s
```

---

## 高级配置

在 `agent.yaml` 的 `advanced` 段控制框架子系统的行为。所有字段都有默认值，不配置即可运行。

### 记忆

```yaml
advanced:
  memory:
    store: file                 # file | sqlite | none
    workspace: ./memory
    retriever: llm              # llm | recent_first
    writer: llm                 # llm | rule
    max_tokens: 2000
    top_k: 5
    model: quick                # 用哪个模型做记忆抽取和检索
    temperature: 0.3
    thinking_enabled: false
```

开启后，框架每轮对话自动提取事实、偏好和决策，存入 `memory/memory.json`。下一轮自动检索相关记忆注入 system prompt。

### 模型路由

```yaml
advanced:
  routing:
    strategy: two_tier          # two_tier | static
    default: quick
    classify:
      medium: quick
      complex: deep
    fallback:
      deep: quick
```

当配置了多个模型时，框架自动启用二级路由：廉价 LLM 对每次用户 query 分类（medium→quick，complex→deep），每轮可动态切换模型。

### 上下文压缩

```yaml
advanced:
  compaction:
    strategy: sliding_window    # sliding_window | none
    threshold: 0.75
```

上一轮 token 用量超过模型上下文窗口的 75% 时，框架自动压缩旧轮次为结构化摘要，保留最近 4 条消息。长工具输出自动落盘。

### 权限

```yaml
advanced:
  permissions:
    deny: [python_exec, file_deleter]
    ask: [file_writer]
    allow: [file_reader, web_search, web_fetch]
```

框架在每次工具调用前执行 deny→ask→allow 检查。`deny` 列表硬阻断，`ask` 列表预留审批通道，`allow` 列表自动放行。未命中任何列表的工具默认走 `ask`。

### 沙箱

```yaml
advanced:
  guardrails:
    input: none                 # none | regex_block | llm_classifier
    output: regex_clean         # none | regex_clean | llm_classifier
    tool_params: path_check     # none | path_check | command_check
```

`PathCheckToolGuard`（`tool_params: path_check`）在每次工具调用前阻断路径穿越（`..`）、绝对路径和工作区逃逸。

### 完整配置示例

```yaml
name: my_agent
description: 带完整高级配置的 ARF 智能体

models:
  - name: quick
    model: deepseek-v4-flash
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 800000
  - name: deep
    model: deepseek-v4-pro
    api_base: https://api.deepseek.com
    api_key_env: DEEPSEEK_API_KEY
    context_window: 1000000

tools:
  - name: file_reader
    description: 读取文件或列出目录
    parameters:
      type: object
      properties:
        operation: {type: string, enum: [read, list]}
        path: {type: string}
      required: [operation, path]
    activation: kernel

skills:
  - name: resource_scaffold
    description: 创建新的工具或技能资源
    tools: [file_writer, resource_loader]
    activation: discoverable

advanced:
  memory:
    store: file
    retriever: llm
    writer: llm
    model: quick
    temperature: 0.3
    thinking_enabled: false
  routing:
    strategy: two_tier
    default: quick
    classify: {medium: quick, complex: deep}
    fallback: {deep: quick}
  compaction:
    strategy: sliding_window
    threshold: 0.75
  permissions:
    deny: []
    ask: [file_writer]
    allow: [file_reader, web_search, web_fetch]
```

---

## 框架目录参考

以下为 `arf/` 下的框架模块。应用开发者通常不需要修改这些代码，但了解它们有助于理解运行时的行为。

```
arf/
├── agent/          # BaseAgent — 组装所有协议实现，注入 call_model
├── engine/         # GraphEngine — DI 驱动的 Agent 执行循环
├── memory/         # FileMemoryStore, LLMMemoryWriter, LLMMemoryRetriever
├── compaction/     # SlidingWindowCompactor — Token 感知滑动窗口
├── routing/        # TwoTierRouter — 快慢模型调度
├── guardrails/     # PathCheckToolGuard, ToolPermissionChecker
├── observability/  # FileTraceStore, UsageTracker
├── hooks/          # SubprocessHookRunner
├── skills/         # SkillPipeline — 工具依赖检查与执行时序
├── sandbox/        # PathSandbox — 路径合法性校验
├── resources/      # DefaultToolResolver, StaticYamlToolProvider
└── core/           # 协议定义、Pydantic 配置模型、事件类型
```

---

## 定制与覆盖

框架的所有组件通过 `BaseAgent.__init__` 的依赖注入替换。如果你的 App 需要自定义存储后端或记忆策略：

```python
from arf.agent.factory import create_agent
from arf.agent.config import AgentConfig

cfg = AgentConfig.from_yaml("agent.yaml")
agent = create_agent(
    config=cfg,
    memory_store=MyCustomStore(),
    memory_writer=MyCustomWriter(),
)
```

绝大多数情况下，你只需要写 `agent.yaml` + `tools/*/function.py`。覆盖框架默认实现是高级用法，用于需要替换存储后端或接入外部系统的场景。

---

## 进一步阅读

| 文档 | 内容 |
|------|------|
| [内存管理](docs/memory-management.md) | 压缩管道、记忆抽取与检索 |
| [模型路由](docs/model-routing.md) | 快慢分流、system model、KV cache |
| [工具沙箱](docs/tool-sandbox.md) | 路径沙箱、权限分级、Hook 退出码 |
| [并发与死锁](docs/skill-pipeline.md) | Skill Pipeline、工具并行执行 |
| [外部中断](docs/interrupt.md) | 取消、undo、Hook 消息注入 |
| [Trace 可观测性](docs/trace.md) | 事件系统、瀑布流、UsageTracker |
