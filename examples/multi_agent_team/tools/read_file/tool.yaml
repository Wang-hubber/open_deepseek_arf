name: read_file
description: Read a text file from the workspace.
parameters:
  type: object
  properties:
    path:
      type: string
      format: path
      description: File path relative to workspace
  required: [path]