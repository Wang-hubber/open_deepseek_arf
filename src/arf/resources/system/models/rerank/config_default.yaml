name: rerank
description: "重排序模型，用于对检索结果进行精排，是 RAG 的依赖项"
model_type: rerank
required: false
config_template:
  base_url:
    label: "API 地址"
    type: string
    required: true
    placeholder: "https://api.openai.com"
  api_key:
    label: "API 密钥"
    type: password
    required: true
    placeholder: "sk-..."
  model_name:
    label: "模型名称"
    type: string
    required: true
    placeholder: "bge-reranker-v2-m3"
depends_on: []
config_page: "OpenAIConfigForm"
