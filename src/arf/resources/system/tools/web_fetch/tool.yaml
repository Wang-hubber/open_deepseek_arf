name: web_fetch
description: "通过 HTTP GET 获取网页内容，伪装浏览器 User-Agent，返回纯文本"
parameters:
  type: object
  properties:
    url:
      type: string
      description: "要请求的 URL"
    timeout:
      type: integer
      description: "请求超时时间（秒），默认 15"
  required:
    - url
