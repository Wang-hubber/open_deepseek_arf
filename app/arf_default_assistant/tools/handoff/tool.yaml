name: handoff
description: Hand off between agents — forward tasks or return results
parameters:
  type: object
  properties:
    task:
      type: string
      description: "Task description in natural language"
    context:
      type: string
      description: "Additional context for the task"
  required:
    - task
execution:
  sandbox: inherit
  timeout: 30s
activation: kernel
