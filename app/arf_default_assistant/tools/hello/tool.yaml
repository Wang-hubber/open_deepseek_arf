name: hello
description: Returns a greeting "Hello, {name}!"
parameters:
  type: object
  properties:
    input:
      type: string
      description: Input parameter
  required:
    - input
execution:
  sandbox: inherit
  timeout: 30s
activation: discoverable
