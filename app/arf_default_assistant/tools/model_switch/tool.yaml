name: model_switch
description: Switch the model used in the current conversation
parameters:
  type: object
  properties:
    model:
      type: string
      description: "Model name to switch to"
  required:
    - model
execution:
  sandbox: inherit
  timeout: 30s
