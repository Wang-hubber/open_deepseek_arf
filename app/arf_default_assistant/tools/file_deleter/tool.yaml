name: file_deleter
description: Soft-delete a file by renaming it with _deleted suffix
parameters:
  type: object
  properties:
    path:
      type: string
      description: "File path relative to workspace root"
  required:
    - path
execution:
  sandbox: inherit
  timeout: 30s
