name: greeting
description: Outputs 'hello world' as a greeting
parameters:
  type: object
  properties:
    input:
      type: string
      description: Input parameter
  required:
    - input
execution:
  sandbox: inherit
  timeout: 30s
activation: discoverable
