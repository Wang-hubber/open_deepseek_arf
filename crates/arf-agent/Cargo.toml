[package]
name = "arf-agent"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ARF AgentConfig: declarative resource configuration skeleton — pure data"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
