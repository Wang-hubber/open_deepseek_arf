name: error_handler
events:
  - {hook_name: "on_error", event_name: "error", mode: "blocking"}
config:
  max_continuation: 3
  max_compaction: 2
  max_transport_retry: 3
  backoff_base: 1.0
  backoff_max: 30.0
