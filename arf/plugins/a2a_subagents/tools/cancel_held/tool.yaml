name: cancel_held
description: Discard held changes from a conflicting sub-agent task.
parameters:
  type: object
  properties:
    task_id:
      type: string
      description: "The task_id of the held changes to discard."
  required:
    - task_id
execution:
  sandbox: none
  timeout: 10s
activation: kernel
