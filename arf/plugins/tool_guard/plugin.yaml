name: tool_guard
enabled: true
description: "Unified tool permission + security check"
hooks:
  pre_action: blocking
config:
  deny: []
  ask: []
  allow: []
  deny_patterns: []
  sandbox_check: true
