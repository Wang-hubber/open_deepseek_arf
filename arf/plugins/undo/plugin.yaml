name: undo
enabled: true
description: "Round-level checkpoint and rollback — workspace + state snapshot per round"
tools:
  - undo
  - checkpoint_count
skills:
  - round_manager
hooks:
  round_start: blocking
  round_end: blocking
config:
  max_undo_depth: 3
