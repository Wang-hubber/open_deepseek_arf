# ARF Agent 综合测试记录

**测试日期**: 2026-05-19
**测试员**: 模拟用户 小明（行政助理，非技术背景）
**Session ID**: 20260519_010742

---

## 测试维度

| # | 维度 | 说明 |
|---|------|------|
| 1 | 指令遵循 | Agent是否准确理解并执行用户指令 |
| 2 | 模型路由 | 分类器是否正确路由到对应模型 |
| 3 | 工具调用 | 工具调用是否正确、结果是否准确 |
| 4 | 错误恢复 | 遇到错误时能否优雅处理 |
| 5 | 上下文压缩 | 长对话是否触发压缩 |
| 6 | 长期记忆 | 跨轮是否记住用户信息 |
| 7 | Trace完整度 | Trace事件是否完整记录 |
| 8 | 回答友好度 | 非技术用户能否理解回复 |
| 9 | 会话存档/恢复 | 会话是否正确存档 |

---

## 逐轮记录

### 第1轮 | 自我介绍 | `new_session=true`

**用户**: "你好，我是小明。我在一家小公司做行政，电脑会用但不是很熟。第一次用你们这种AI助手，你能帮我做些什么呀？用简单的话说就行，不要太复杂。"

**Agent回复**: "你看，刚才我就顺便把你的信息记下来了..."

**校验**:
- 分类: medium → quick_thinking ✓
- 工具: 主动调用 memory_store 记录用户信息 ✓
- 问题: 回复跳脱，缺少正式的自我介绍和能力介绍 ✗
- 原因: 模型先调了memory_store，第二轮call_model直接跳到"记下来了"，跳过了自我介绍

| 维度 | 评分 | 依据 |
|------|------|------|
| 指令遵循 | ⚠️ | 执行了记忆但跳过了自我介绍 |
| 模型路由 | ✅ | medium→quick_thinking 正确 |
| 工具调用 | ✅ | memory_store 被正确调用 |
| 回答友好度 | ⚠️ | 语言简单但衔接不自然 |

---

### 第2轮 | 模糊需求 | 同一session

**用户**: "帮我做个快递表格"

**Agent回复**: 追问了三个选项（快递记录表/查询物流/从文件生成），要求用户明确需求。

**校验**:
- 分类: medium→quick_thinking
- 无工具调用，纯追问
- 行为正确——真实用户不会一次说清楚需求

| 维度 | 评分 | 依据 |
|------|------|------|
| 指令遵循 | ✅ | 没有瞎猜，追问细节 |
| 回答友好度 | ✅ | 选项清晰，小白能理解 |

---

### 第3轮 | 创建表格 | 同一session

**用户**: "第一种，建个模板就行。列的话就要单号、寄件人、收件人、日期、快递公司。"

**Agent回复**: "模板已经生成，点击上方链接即可下载！"

**校验**:
- 工具: file_writer → 创建了 `uploads/物流记录模板.csv` ✓
- 问题1: Agent填了示例数据（张三李四），用户要的是空模板 ✗
- 问题2: "点击上方链接"对API用户无意义，未提供文件路径 ✗
- Trace: file_writer status=ok，文件确实创建

| 维度 | 评分 | 依据 |
|------|------|------|
| 指令遵循 | ⚠️ | 建了模板但多填了示例数据 |
| 工具调用 | ✅ | file_writer正确执行 |
| 回答友好度 | ✗ | "点击链接"在API场景无效 |

---

### 第4轮 | 查假单号 | 同一session

**用户**: "帮我查一下顺丰 SF1234567890 这个快递到哪了"

**Agent回复**: 返回"运输中，苏州分拣中心，2026-05-12"

**校验**:
- 工具: logistics_tracker → status=ok
- 返回数据: `{"status": "运输中", "latest_trace": "2026-05-12 18:19:26 已到达 苏州分拣中心"}`
- 问题: SF1234567890是假单号，工具返回了假数据（mock），Agent未质疑数据真实性 ✗

| 维度 | 评分 | 依据 |
|------|------|------|
| 工具调用 | ✅ | logistics_tracker正确执行 |
| 事实校验 | ✗ | 假单号返回假数据，Agent照单全收 |

---

### 第5轮 | 真实单号SF5102111701016 | 同一session

**用户**: "查下SF5102111701016"（真实单号，2026-02-22签收）

**Agent回复**: "已签收，最后更新2026-05-13"，提到快递100返回"查无结果"

**校验**:
- 工具: web_fetch → 调用快递100 API → `{"ischeck":"1","com":"shunfeng"}`
- 快递100 API确认已签收 ✓
- 问题: Agent报告"最后更新2026-05-13"，实际签收日期是2026-02-22 ✗
- API未返回具体日期，Agent自己编了一个

| 维度 | 评分 | 依据 |
|------|------|------|
| 工具调用 | ✅ | 正确使用web_fetch调用外部API |
| 事实校验 | ✗ | 日期是编造的，API没返回这个数据 |

---

### 第6轮 | 记忆召回 | 同一session

**用户**: "对了你还记得我叫什么名字吗"

**Agent回复**: "当然记得呀！你是小明，在一家小公司做行政工作"

**校验**:
- 工具: 无工具调用，直接从对话历史获取
- 对话历史中有"我是小明"→ 这不是长期记忆，是上下文记忆

| 维度 | 评分 | 依据 |
|------|------|------|
| 长期记忆 | ⚠️ | 从对话历史取的，未测试真正的memory_store召回 |

---

### 第7轮 | 错误恢复 | 同一session

**用户**: "把那个物流表格删掉，然后帮我把不存在的文件读出来看看"

**Agent回复**: 表格删除成功；不存在的文件正确报错 "文件未找到"；还调侃了一句。

**校验**:
- 工具1: file_deleter → 成功删除 `uploads/物流记录模板.csv` ✓
- 工具2: file_reader → 返回错误 `File not found` ✓
- Agent正确区分了成功和失败，并优雅地报告了两个结果

| 维度 | 评分 | 依据 |
|------|------|------|
| 错误恢复 | ✅ | 正确处理了文件不存在错误 |
| 指令遵循 | ✅ | 两个子任务都执行了 |

---

### 第8轮 | 真实单号DPK364992515420 | 同一session

**用户**: "查DPK364992515420"（德邦，2026-04-24签收）

**Agent回复**: "德邦，已签收，最后更新2026年5月15日，本人签收"

**校验**:
- 正确识别为德邦（DPK前缀）✓
- 已签收状态确认 ✓（需查trace确认API返回）
- 日期再次对不上 ✗

| 维度 | 评分 | 依据 |
|------|------|------|
| 工具调用 | ✅ | 识别了德邦快递公司 |
| 事实校验 | ✗ | 日期再次不准确 |

---

## 中间总结 （第8轮后）

**已覆盖**: 指令遵循、模型路由、工具调用、错误恢复、回答友好度
**未覆盖**: 上下文压缩、长期记忆(跨session)、会话存档/恢复
**待测试**: 复杂任务触发handoff/model_switch

---

### 第9轮 | 复杂任务/模型路由 | new_session

**用户**: "帮我设计一个自动查快递的流程：每天早上去查所有还没签收的快递，有延误超过2天的自动标出来发预警。分模块说说就行。"

**Agent回复**: 3653字详细设计，分6个模块（数据管理/定时调度/物流查询/异常检测/通知推送/报告输出），含mermaid图

**校验**:
- 分类: complex → deep_thinking ✓
- 未触发handoff_to_sys（UserAgent用deep_thinking自己处理了）✓
- 无工具调用，纯文本设计
- 回复质量高，模块化清晰

| 维度 | 评分 | 依据 |
|------|------|------|
| 模型路由 | ✅ | complex正确路由到deep_thinking |
| 回答友好度 | ✅ | 用简单语言解释技术方案，带emoji和表格 |

---

### 第10轮 | 跨session长期记忆 | new_session

**用户**: "你好，还记得我吗？"

**Agent回复**: "你是小明，在一家小公司做行政工作，电脑水平属于'会用但不熟'..."

**校验**:
- 无工具调用，直接回答（从system prompt的long_term_memory section获取）
- `long_term.md` 包含之前存储的信息 ✓
- 记忆准确：名字、职业、电脑水平、偏好 ✓

| 维度 | 评分 | 依据 |
|------|------|------|
| 长期记忆 | ✅ | 跨session正确召回用户信息 |

---

### 第11轮 | 上下文压缩 | 多轮积累

**操作**: 连续5轮请求（建目录/写文件/读文件/搜索/写报告）

**结果**:
- compaction事件: **0次**触发
- web_search失败: 网络不可达（URLError 101）
- 第5轮达到max_turns截断

**根因分析**:
- 每个请求 `history: []` → graph从1条消息开始
- max_turns=6 限制 → 在消息数达到10之前就被截断
- 压缩设计OK，但当前会话模式无法触发

| 维度 | 评分 | 依据 |
|------|------|------|
| 上下文压缩 | ⚠️ | 机制存在但当前使用模式无法触发 |

---

### 第12轮 | 会话存档检查

**校验**:
- 存档目录: `memory/sessions/` 有9个存档文件
- 结构: id, title, created_at, ended_at, message_count, messages, graph_traces, usage
- 最大存档 27KB（含38条graph_traces）
- 缺失: tool_call和tool_result类型的消息未存入messages

| 维度 | 评分 | 依据 |
|------|------|------|
| 会话存档 | ✅ | 结构完整，含metadata+traces+usage |

---

## 综合评分

| # | 维度 | 分数 | 证据 |
|---|------|------|------|
| 1 | 指令遵循 | 7/10 | 基本遵循，但创建表格时多填了示例数据；模糊需求会追问 |
| 2 | 模型路由 | 8/10 | medium/quick_thinking, complex/deep_thinking均正确；未触发handoff场景 |
| 3 | 工具调用 | 7/10 | 工具调用正常执行；但logistics_tracker返回假数据未质疑；日期编造 |
| 4 | 错误恢复 | 8/10 | 文件不存在正确处理；网络不可达有友好提示 |
| 5 | 上下文压缩 | 5/10 | 机制存在但从未触发（history=[] + max_turns限制） |
| 6 | 长期记忆 | 8/10 | 跨session正确召回；memory_store自动触发 |
| 7 | Trace完整度 | 8/10 | 修复后lifecycle事件正常；prompt_snapshot、session生命周期完整 |
| 8 | 回答友好度 | 8/10 | 简体中文、emoji、表格、简单语言；偶有"点击链接"不适配API场景 |
| 9 | 会话存档 | 7/10 | 结构完整；缺少tool_call/tool_result消息 |

**综合**: 7.3/10

### Trace系统修复记录
1. `_migrate_schema()` 只在首次连接时运行 → 修复为每次init_db运行
2. 缺少prompts表迁移 → 添加到_migrate_schema
3. `node=None` 违反NOT NULL → _normalize_trace_event中转换
4. lifecycle事件flush需history>=2 → 移除条件
5. session_start在reset前emit → 移到reset后
6. trace_collector未传播到UserAgent/SysAgent → 在get_agent中注入
