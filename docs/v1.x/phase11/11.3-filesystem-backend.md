# Phase 11 / 11.3 — `FilesystemBackend` 真实文件系统后端

> **Task**: 实现 `FilesystemBackend` — 真实 FS 操作 + 路径 sandbox。
> **Gap addressed**: G-01 子项 3, G-14 (filesystem permission interrupt)
> **Source**: `docs/v1.x/phase10/99-gap-summary-recommendations.md` §3 G-01
> **Reference**: DeepAgents `FilesystemBackend` — `libs/deepagents/deepagents/backends/filesystem.py`
> **Status**: 📝 Doc — awaiting user Gitee 精校

---

## 1. Motivation

`StateBackend`（11.2）是安全的内存默认。但实际应用需要**真实文件系统**：
- 读取已有项目源码
- 写入构建产物
- 长任务 checkpoint 文件

ARFV1 当前通过 `McpNode::local` 直接用 host FS，无 sandbox 抽象。FilesystemBackend 提供：
- **统一接口** 与 StateBackend 一致，可互换
- **路径 sandbox**：限定 root 目录，路径逃逸被拒绝
- **真实 FS 语义**：tokio::fs + globset + grep-regex

---

## 2. Scope

### ✅ In scope

1. `FilesystemBackend` 结构 + `BackendProtocol` 实现
2. 路径 sandbox：所有操作限定在 `root_dir` 内，路径逃逸返回 `PermissionDenied`
3. 9 个 trait 方法的完整实现
4. `globset` 替换 inline glob（StateBackend 的）
5. `grep-regex` 替换 inline substring search
6. Symlink 安全：`metadata().is_symlink()` 检查
7. 完整测试

### ❌ Out of scope

- FilesystemMiddleware 接入（→ Phase 11-M2 / G-02）
- SandboxBackendProtocol 实现（→ M4 / G-04 — `LocalShellBackend`）
- 远程 FS（S3 / NFS）— 后续 task

---

## 3. Crate 依赖增量

```toml
[dependencies]
# ... 沿用 11.1 + 11.2
tokio = { version = "1", features = ["fs"] }
globset = "0.4"
grep-regex = "0.1"
walkdir = "2"
```

- `tokio::fs` — 异步文件 IO
- `globset` — 比 inline glob 正确、性能好
- `grep-regex` — 快速文本搜索
- `walkdir` — 目录遍历（glob 实现）

---

## 4. 完整代码

### 4.1 `src/filesystem.rs`

```rust
//! `FilesystemBackend` — real-FS backend with root-directory sandboxing.
//!
//! Maps `BackendPath` (`/foo/bar`) onto `<root_dir>/foo/bar` on the host
//! filesystem. All operations are constrained to `root_dir`; any attempt
//! to escape (via `..`, absolute paths outside root, or symlinks pointing
//! outside root) returns `BackendError::PermissionDenied`.

use std::path::{Component, Path, PathBuf};
use std::sync::Arc;

use async_trait::async_trait;
use chrono::{DateTime, Utc};
use globset::{Glob, GlobMatcher};
use tokio::fs;
use tokio::sync::Mutex;

use crate::error::BackendError;
use crate::path::BackendPath;
use crate::protocol::BackendProtocol;
use crate::types::{
    DeleteResult, EditResult, FileData, FileInfo, GlobResult, GrepMatch, GrepResult, LsResult,
    ReadResult, WriteResult,
};

/// Real-FS backend rooted at a single directory.
#[derive(Clone)]
pub struct FilesystemBackend {
    /// Absolute path to the sandbox root.
    root: PathBuf,
    /// Cached glob matchers (avoid recompiling on each call).
    glob_cache: Arc<Mutex<Vec<(String, GlobMatcher)>>>,
}

impl FilesystemBackend {
    /// Create a `FilesystemBackend` rooted at `root_dir`.
    ///
    /// `root_dir` MUST be an absolute, existing directory. The backend
    /// refuses operations that escape this root.
    pub fn new(root_dir: impl Into<PathBuf>) -> Result<Self, BackendError> {
        let root = root_dir.into();
        if !root.is_absolute() {
            return Err(BackendError::InvalidPath {
                path: root.to_string_lossy().into(),
                code: "invalid_path",
            });
        }
        // Verify the root exists and is a directory. We use std::fs (sync)
        // here because this is a one-time startup check; per-call ops use
        // tokio::fs.
        let meta = std::fs::metadata(&root).map_err(|e| BackendError::Io(e.to_string()))?;
        if !meta.is_dir() {
            return Err(BackendError::Io(format!(
                "root is not a directory: {}",
                root.display()
            )));
        }
        Ok(Self {
            root,
            glob_cache: Arc::new(Mutex::new(Vec::new())),
        })
    }

    /// Return the absolute root path (for debugging/tests).
    pub fn root(&self) -> &Path {
        &self.root
    }

    /// Map a `BackendPath` to its on-disk absolute path, verifying that
    /// the resolved path stays inside the root.
    ///
    /// This is the security-critical function. All other methods route
    /// through here so the sandbox invariant is enforced in exactly one
    /// place.
    async fn resolve(&self, path: &BackendPath) -> Result<PathBuf, BackendError> {
        let stripped = path.as_str().trim_start_matches('/');
        let candidate = self.root.join(stripped);
        // Canonicalize (resolve `..` and symlinks). If the file doesn't
        // exist yet, canonicalize fails — fall back to lexical
        // normalization then prefix check.
        let canonical = match fs::canonicalize(&candidate).await {
            Ok(p) => p,
            Err(_) => {
                // File may not exist (write to new path). Normalize lexically.
                normalize_lexically(&candidate)
            }
        };
        // Strip the canonical root's trailing separator if any.
        let root_canonical = fs::canonicalize(&self.root)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        if !canonical.starts_with(&root_canonical) {
            return Err(BackendError::PermissionDenied {
                path: path.to_string(),
                reason: format!("path escapes root: {}", canonical.display()),
                code: "permission_denied",
            });
        }
        Ok(canonical)
    }

    /// Compile a glob pattern, caching the result.
    async fn compile_glob(&self, pattern: &str) -> Result<GlobMatcher, BackendError> {
        // Fast path: check cache.
        {
            let cache = self.glob_cache.lock().await;
            if let Some((_, matcher)) = cache.iter().find(|(p, _)| p == pattern) {
                return Ok(matcher.clone());
            }
        }
        // Compile and insert.
        let glob =
            Glob::new(pattern).map_err(|e| BackendError::Io(format!("bad glob: {e}")))?;
        let matcher = glob.compile_matcher();
        let mut cache = self.glob_cache.lock().await;
        cache.push((pattern.to_string(), matcher.clone()));
        Ok(matcher)
    }
}

// ---------------------------------------------------------------------------
// Trait impl
// ---------------------------------------------------------------------------

#[async_trait]
impl BackendProtocol for FilesystemBackend {
    async fn ls(&self, path: &BackendPath) -> Result<LsResult, BackendError> {
        let abs = self.resolve(path).await?;
        let meta = fs::metadata(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        if !meta.is_dir() {
            return Err(BackendError::NotADirectory {
                path: path.to_string(),
            });
        }
        let mut entries = fs::read_dir(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        let mut results = Vec::new();
        while let Some(entry) = entries
            .next_entry()
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?
        {
            let file_name = entry.file_name();
            let entry_path = path.join_str(&file_name.to_string_lossy());
            let meta = entry
                .metadata()
                .await
                .map_err(|e| BackendError::Io(e.to_string()))?;
            let modified_at: Option<DateTime<Utc>> = meta
                .modified()
                .ok()
                .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                .map(|d| {
                    DateTime::<Utc>::from_timestamp(d.as_secs() as i64, d.subsec_nanos()).unwrap()
                });
            results.push(FileInfo {
                path: entry_path.as_str().to_string(),
                is_dir: meta.is_dir(),
                size: meta.len(),
                modified_at,
            });
        }
        Ok(LsResult {
            error: None,
            entries: Some(results),
        })
    }

    async fn read(
        &self,
        path: &BackendPath,
        offset: u32,
        limit: u32,
    ) -> Result<ReadResult, BackendError> {
        let abs = self.resolve(path).await?;
        let meta = fs::metadata(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        if meta.is_dir() {
            return Err(BackendError::IsDirectory {
                path: path.to_string(),
                code: "is_directory",
            });
        }
        // Refuse symlinks (security: avoid info leak + escape).
        if meta.file_type().is_symlink() {
            return Err(BackendError::PermissionDenied {
                path: path.to_string(),
                reason: "symlinks not followed for read".into(),
                code: "permission_denied",
            });
        }
        let bytes = fs::read(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        let content = match String::from_utf8(bytes.clone()) {
            Ok(s) => s,
            Err(_) => {
                // Binary file — base64 encode (not implemented here; surface error).
                return Ok(ReadResult {
                    error: Some("file is binary; base64 encoding not yet supported".into()),
                    file_data: None,
                });
            }
        };
        // Apply offset/limit on lines.
        let lines: Vec<&str> = content.lines().collect();
        let start = (offset as usize).min(lines.len());
        let end = if limit == 0 {
            lines.len()
        } else {
            (start + limit as usize).min(lines.len())
        };
        let slice = lines[start..end].join("\n");
        let modified_at: Option<DateTime<Utc>> = meta
            .modified()
            .ok()
            .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
            .map(|d| DateTime::<Utc>::from_timestamp(d.as_secs() as i64, d.subsec_nanos()).unwrap());
        Ok(ReadResult {
            error: None,
            file_data: Some(FileData {
                content: slice,
                encoding: "utf-8".into(),
                created_at: meta
                    .created()
                    .ok()
                    .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                    .and_then(|d| {
                        DateTime::<Utc>::from_timestamp(d.as_secs() as i64, d.subsec_nanos())
                    }),
                modified_at,
            }),
        })
    }

    async fn write(&self, path: &BackendPath, content: &str) -> Result<WriteResult, BackendError> {
        let abs = self.resolve(path).await?;
        // Create parent dirs if missing.
        if let Some(parent) = abs.parent() {
            fs::create_dir_all(parent)
                .await
                .map_err(|e| BackendError::Io(e.to_string()))?;
        }
        fs::write(&abs, content)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        Ok(WriteResult {
            error: None,
            path: Some(path.to_string()),
        })
    }

    async fn edit(
        &self,
        path: &BackendPath,
        old_string: &str,
        new_string: &str,
        replace_all: bool,
    ) -> Result<EditResult, BackendError> {
        let abs = self.resolve(path).await?;
        let content = fs::read_to_string(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        let occurrences = content.matches(old_string).count();
        if occurrences == 0 {
            return Err(BackendError::EditMatch {
                reason: format!("old_string not found in {path}"),
            });
        }
        if !replace_all && occurrences > 1 {
            return Err(BackendError::EditMatch {
                reason: format!(
                    "old_string matched {occurrences} times in {path} (not unique)"
                ),
            });
        }
        let new_content = if replace_all {
            content.replace(old_string, new_string)
        } else {
            content.replacen(old_string, new_string, 1)
        };
        let actual_replacements = if replace_all {
            occurrences as u32
        } else {
            1
        };
        fs::write(&abs, new_content)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        Ok(EditResult {
            error: None,
            path: Some(path.to_string()),
            occurrences: actual_replacements,
        })
    }

    async fn delete(&self, path: &BackendPath) -> Result<DeleteResult, BackendError> {
        let abs = self.resolve(path).await?;
        let meta = fs::metadata(&abs)
            .await
            .map_err(|e| BackendError::Io(e.to_string()))?;
        if meta.is_dir() {
            fs::remove_dir_all(&abs)
                .await
                .map_err(|e| BackendError::Io(e.to_string()))?;
        } else {
            fs::remove_file(&abs)
                .await
                .map_err(|e| BackendError::Io(e.to_string()))?;
        }
        Ok(DeleteResult {
            error: None,
            path: Some(path.to_string()),
        })
    }

    async fn glob(
        &self,
        pattern: &str,
        base: Option<&BackendPath>,
    ) -> Result<GlobResult, BackendError> {
        let base_abs = match base {
            Some(p) => Some(self.resolve(p).await?),
            None => None,
        };
        let base_abs = base_abs.as_deref().unwrap_or(&self.root);
        let matcher = self.compile_glob(pattern).await?;
        let mut results = Vec::new();
        for entry in walkdir::WalkDir::new(base_abs)
            .follow_links(false)
            .into_iter()
            .filter_entry(|e| !is_hidden(e))
        {
            let entry = entry.map_err(|e| BackendError::Io(e.to_string()))?;
            let path = entry.path();
            if !matcher.is_match(path) {
                continue;
            }
            // Map back to BackendPath (relative to root).
            let rel = path.strip_prefix(&self.root).unwrap_or(path);
            let backend_path = BackendPath::new(&format!("/{}", rel.to_string_lossy())).unwrap_or_else(|_| {
                // If the path can't be normalized (contains `..` somehow),
                // skip it.
                BackendPath::new("/_invalid_").unwrap()
            });
            let meta = entry
                .metadata()
                .map_err(|e| BackendError::Io(e.to_string()))?;
            let modified_at: Option<DateTime<Utc>> = meta
                .modified()
                .ok()
                .and_then(|t| t.duration_since(std::time::UNIX_EPOCH).ok())
                .map(|d| DateTime::<Utc>::from_timestamp(d.as_secs() as i64, d.subsec_nanos()).unwrap());
            results.push(FileInfo {
                path: backend_path.as_str().to_string(),
                is_dir: meta.is_dir(),
                size: meta.len(),
                modified_at,
            });
        }
        Ok(GlobResult {
            error: None,
            matches: Some(results),
            truncated: false,
        })
    }

    async fn grep(
        &self,
        pattern: &str,
        path: Option<&BackendPath>,
        glob_filter: Option<&str>,
    ) -> Result<GrepResult, BackendError> {
        let base = match path {
            Some(p) => self.resolve(p).await?,
            None => self.root.clone(),
        };
        let glob_matcher = match glob_filter {
            Some(g) => Some(self.compile_glob(g).await?),
            None => None,
        };
        let matcher = grep_regex::RegexMatcher::new(pattern)
            .map_err(|e| BackendError::Io(format!("bad grep pattern: {e}")))?;
        let mut results = Vec::new();
        for entry in walkdir::WalkDir::new(&base).follow_links(false) {
            let entry = entry.map_err(|e| BackendError::Io(e.to_string()))?;
            let entry_path = entry.path();
            if !entry_path.is_file() {
                continue;
            }
            // globset's is_match expects a path-like; relative-to-root is best.
            let rel = entry_path.strip_prefix(&self.root).unwrap_or(entry_path);
            if let Some(ref m) = glob_matcher {
                if !m.is_match(rel) {
                    continue;
                }
            }
            // grep-regex requires a sync Searcher; run in spawn_blocking.
            let matcher = matcher.clone();
            let path_clone = entry_path.to_path_buf();
            let rel_owned = rel.to_path_buf();
            let search_result = tokio::task::spawn_blocking(move || {
                let mut searcher = grep_regex::RegexSearcher::new(
                    &matcher,
                    grep_regex::Sink::new(|_, line| {
                        let text = String::from_utf8_lossy(line).into_owned();
                        Some(text)
                    }),
                );
                // Read file content (sync since spawn_blocking).
                let content = std::fs::read(&path_clone)?;
                let mut matches = Vec::new();
                searcher.search_slice(&content, |line_num, line| {
                    matches.push((line_num, line.to_string()));
                    Ok(())
                })?;
                Ok::<_, Box<dyn std::error::Error + Send + Sync>>(matches)
            })
            .await
            .map_err(|e| BackendError::Io(format!("spawn_blocking join: {e}")))?
            .map_err(|e| BackendError::Io(e.to_string()))?;
            for (line_num, line) in search_result {
                let backend_path = BackendPath::new(&format!(
                    "/{}",
                    rel_owned.to_string_lossy()
                ))
                .map_err(|e| BackendError::Io(e.to_string()))?;
                results.push(GrepMatch {
                    path: backend_path.as_str().to_string(),
                    line: line_num as u32,
                    text: line,
                });
            }
        }
        Ok(GrepResult {
            error: None,
            matches: Some(results),
            truncated: false,
        })
    }

    async fn upload_files(
        &self,
        files: Vec<(BackendPath, Vec<u8>)>,
    ) -> Result<Vec<Result<(), BackendError>>, BackendError> {
        let mut results = Vec::with_capacity(files.len());
        for (path, bytes) in files {
            let abs = match self.resolve(&path).await {
                Ok(p) => p,
                Err(e) => {
                    results.push(Err(e));
                    continue;
                }
            };
            if let Some(parent) = abs.parent() {
                if let Err(e) = fs::create_dir_all(parent).await {
                    results.push(Err(BackendError::Io(e.to_string())));
                    continue;
                }
            }
            if let Err(e) = fs::write(&abs, &bytes).await {
                results.push(Err(BackendError::Io(e.to_string())));
                continue;
            }
            results.push(Ok(()));
        }
        Ok(results)
    }
}

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

/// Lexical normalization (resolve `.` and `..` without touching disk).
/// Used when canonicalize fails (file doesn't exist yet).
fn normalize_lexically(path: &Path) -> PathBuf {
    let mut result = PathBuf::new();
    for component in path.components() {
        match component {
            Component::Prefix(_) | Component::RootDir => result.push(component),
            Component::CurDir => {}
            Component::ParentDir => {
                if !result.pop() {
                    // Already at root; keep ParentDir to surface the escape.
                    result.push("..");
                }
            }
            Component::Normal(c) => result.push(c),
        }
    }
    result
}

/// Skip hidden files (starting with `.`) during directory traversal.
fn is_hidden(entry: &walkdir::DirEntry) -> bool {
    entry
        .file_name()
        .to_str()
        .map(|s| s.starts_with('.'))
        .unwrap_or(false)
}

// ---------------------------------------------------------------------------
// BackendPath extension (for ls to construct child paths)
// ---------------------------------------------------------------------------

trait BackendPathExt {
    fn join_str(&self, segment: &str) -> BackendPath;
}

impl BackendPathExt for BackendPath {
    fn join_str(&self, segment: &str) -> BackendPath {
        let sep = if self.as_str().ends_with('/') { "" } else { "/" };
        let raw = format!("{}{}{}", self.as_str(), sep, segment);
        BackendPath::new(&raw).unwrap_or_else(|_| self.clone())
    }
}
```

> **设计取舍**：
> - `resolve()` 是唯一路径映射入口——其他方法都通过它，确保 sandbox 不变量集中在一处
> - `canonicalize` 优先（解析 symlinks）；失败则 lexical normalize（新文件）
> - symlink **不被 follow** for read，避免路径逃逸
> - `globset` + `walkdir` 替换 inline glob——性能更好，正确性更可靠
> - `grep-regex` + `spawn_blocking`——grep 本质是 CPU-bound，不阻塞 async runtime

### 4.2 `src/error.rs` 增量

添加新变体：

```rust
/// Path exists but is not a directory (when directory was expected).
#[error("not a directory: {path}")]
NotADirectory { path: String },
```

### 4.3 `src/lib.rs` 增量

```rust
pub mod filesystem;

pub use filesystem::FilesystemBackend;
```

---

## 5. 逐行解释

### 5.1 路径 sandbox（`resolve` 方法）

```rust
let canonical = match fs::canonicalize(&candidate).await { ... };
let root_canonical = fs::canonicalize(&self.root).await ...;
if !canonical.starts_with(&root_canonical) {
    return Err(BackendError::PermissionDenied { ... });
}
```

关键：**必须用 canonicalize 后比较**，不能用字符串 starts_with。否则：
- `/root/foo/../../etc/passwd` 字符串匹配会通过，但实际访问 host /etc/passwd
- canonicalize 解析所有 `..` 和 symlinks

### 5.2 symlink 安全

`read()` 拒绝 symlink：避免 path canonicalize 跳到 sandbox 外。**注意：resolve 已经 canonicalize 过了，所以 symlink 在 resolve 阶段就被解析；这里再拒绝是第二层防御（fail-closed）。**

### 5.3 grep `spawn_blocking`

`grep-regex` 是 sync API。直接 await 会阻塞 tokio runtime。通过 `spawn_blocking` 移到 thread pool：

```rust
let search_result = tokio::task::spawn_blocking(move || { ... }).await ...
```

### 5.4 glob 缓存

`globset::Glob::compile_matcher()` 不便宜——同一 pattern 反复编译浪费。`glob_cache: Arc<Mutex<...>>` 缓存最近编译结果。FilesystemBackend 通常每次执行只用几个 pattern。

---

## 6. 测试设计

### 6.1 测试文件：`crates/arf-backend/tests/filesystem_backend.rs`

| ID | 角度 | 测试内容 | 期望 |
|----|------|---------|------|
| T-F01 | [构造] | `new(root)` 接受绝对路径 | 成功 |
| T-F02 | [构造] | `new("relative")` | `Err(InvalidPath)` |
| T-F03 | [构造] | `new("/nonexistent")` | `Err(Io)` |
| T-F04 | [构造] | `new(file)` 路径是文件而非目录 | `Err(Io)` |
| T-F05 | [方法] | write → read 真实文件 | 内容一致 |
| T-F06 | [方法] | write 创建父目录 | 嵌套路径自动创建 |
| T-F07 | [方法] | read 不存在的文件 | `error: "file_not_found"` |
| T-F08 | [方法] | ls 真实目录 | 返回 entries |
| T-F09 | [方法] | ls 文件而非目录 | `Err(NotADirectory)` |
| T-F10 | [方法] | edit 唯一匹配 | 写入成功 |
| T-F11 | [方法] | edit 多次匹配不传 replace_all | `Err(EditMatch)` |
| T-F12 | [方法] | delete 文件 | 文件从磁盘消失 |
| T-F13 | [方法] | delete 目录 | 递归删除 |
| T-F14 | [方法] | glob `**/*.rs` 在真实目录 | 匹配正确 |
| T-F15 | [方法] | grep pattern | 返回匹配行 |
| T-F16 | [方法] | upload_files batch | 写入文件 |
| T-F17 | [边界] | 路径 sandbox: `/../etc/passwd` | `Err(PermissionDenied)` |
| T-F18 | [边界] | 路径 sandbox: `..` 在中间 | `Err(PermissionDenied)` |
| T-F19 | [边界] | 路径 sandbox: 绝对路径在 root 外 | `Err(PermissionDenied)` |
| T-F20 | [边界] | symlink 指向 sandbox 外 | `Err(PermissionDenied)` (read) |
| T-F21 | [边界] | read 二进制文件 | `error: binary not supported` |
| T-F22 | [并发] | 并发 write 不同文件 | 全部成功 |
| T-F23 | [trait] | `Arc<dyn BackendProtocol>` | compile OK |
| T-F24 | [trait] | `Box<dyn BackendProtocol>` | compile OK |

---

## 7. 与 DeepAgents FilesystemBackend 的差异

| 维度 | DeepAgents | ARFV1 | 原因 |
|------|------------|-------|------|
| FS 库 | `pathlib.Path` | `tokio::fs` | async-first |
| 路径 sandbox | `root_dir` 参数 + `pathlib.resolve()` | `resolve()` 方法 + canonicalize | 集中一处 |
| Symlink | 默认 follow | **不** follow | ARF fail-closed 原则 |
| Glob | `globset` + `pathlib.glob` | `globset` + `walkdir` | 一致 |
| Grep | `ripgrep` 后备 | `grep-regex`（纯 Rust） | 无外部依赖 |
| `upload_files` | bytes | bytes | 一致 |

---

## 8. 不破坏 ARFV1 现有优势

1. **McpNode::local 仍然可用** — FilesystemBackend 是新抽象，不替代现有 McpNode
2. **Protocol DI** — `FilesystemBackend: BackendProtocol`，可与 StateBackend 互换
3. **异步优先** — 所有 IO async（grep 内部用 spawn_blocking）

---

## 9. 实现计划

1. 写 doc（本任务）→ push
2. 批准后：
   - `Cargo.toml` 加 `globset` + `grep-regex` + `walkdir` + tokio fs（1 commit）
   - `src/error.rs` 加 `NotADirectory` 变体（1 commit，与 state.rs 同一文件；可合并）
   - `src/filesystem.rs`（1 commit）
   - `src/lib.rs` 添加 `pub mod filesystem;`（1 commit）
   - `tests/filesystem_backend.rs`（1 commit）

预计 4 个原子 commit。

---

## 10. 风险与备选

| 风险 | 影响 | 备选 |
|------|------|------|
| `canonicalize` 慢 | 高频 read 性能 | cache resolve 结果（M3 再加） |
| `walkdir` 不支持 follow_links(true) | 隐藏 .git 等 | `follow_links(false)` + filter_entry hidden |
| grep spawn_blocking 阻塞线程 | 高并发下阻塞 thread pool | 加并发限制（`buffer_unordered`） |
| 二进制文件 base64 | 当前未实现 | M2 接入 FilesystemMiddleware 时加 |

---

## 11. 参考资料

- `docs/v1.x/phase11/11.1-backend-protocol.md`
- `docs/v1.x/phase11/11.2-state-backend.md`
- `docs/v1.x/phase10/05-filesystem-tool-surface.md`
- DeepAgents `libs/deepagents/deepagents/backends/filesystem.py`
- `crates/arf-mcp/src/runtime.rs:19-50` — 现有 RuntimeModule（不替换）

---

## 版本

- **Phase**: 11
- **Task**: 11.3 (FilesystemBackend)
- **Doc version**: v1
- **Date**: 2026-07-06
- **下一步**: 实现 + 测试 + commit/push