# Phase 11 / 11.5 — `BackendFactory` + `_resolve_backend`

> **Task**: 实现 `BackendFactory` — 工厂 + 环境变量解析 + 注册表。
> **Gap addressed**: G-01 子项 (factory 装配)
> **Source**: `docs/v1.x/phase10/99-gap-summary-recommendations.md` §3 G-01
> **Reference**: DeepAgents `BackendFactory` + `_resolve_backend` —
>               `libs/deepagents/deepagents/backends/protocol.py:982-992`
> **Status**: 📝 Doc — awaiting user Gitee 精校

---

## 1. Motivation

应用需要一个统一入口创建 backend：
- 从环境变量 `ARF_BACKEND=filesystem:./workspace` 解析
- 缺省值（StateBackend / FilesystemBackend）选择
- 注册自定义 backend 类型（future: LangSmith / E2B）

ARFV1 当前**没有 factory**——每个 app 自己写 `new()` 逻辑。

**本任务目标**：实现 `BackendFactory` + `_resolve_backend`，提供统一入口。

---

## 2. Scope

### ✅ In scope

1. `BackendFactory::from_env()` — 从 `ARF_BACKEND` env 解析
2. `BackendFactory::default()` — StateBackend 默认
3. `_resolve_backend(backend, ctx)` — 接受 backend 实例或 factory callable
4. 简单注册表（不支持 entry point plugin，那是 G-16 后续）
5. 完整测试

### ❌ Out of scope

- Entry point plugin 系统（→ G-16 / Phase 12-M1）
- Profile merge（→ G-51 / Phase 12-M2）

---

## 3. 完整代码

### 3.1 `src/factory.rs`

```rust
//! `BackendFactory` — env-driven backend construction (Phase 11 / 11.5).
//!
//! Single entry point for apps that need a `BackendProtocol`. Resolves
//! `ARF_BACKEND=...` env var to a concrete backend instance.

use std::sync::Arc;

use crate::error::BackendError;
use crate::filesystem::FilesystemBackend;
use crate::path::BackendPath;
use crate::protocol::BackendProtocol;
use crate::state::StateBackend;

/// Result of parsing an `ARF_BACKEND` env value.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum BackendSpec {
    /// In-memory StateBackend (no config).
    State,
    /// FilesystemBackend rooted at the given path.
    Filesystem(String),
    /// Unknown backend type — surfaces as error at construction.
    Unknown(String),
}

impl BackendSpec {
    /// Parse an `ARF_BACKEND` value string into a `BackendSpec`.
    ///
    /// Format: `<type>[:<config>]`
    /// - `state` — StateBackend
    /// - `filesystem:<path>` — FilesystemBackend rooted at `<path>`
    pub fn parse(raw: &str) -> Self {
        let (ty, cfg) = match raw.split_once(':') {
            Some((t, c)) => (t, c),
            None => (raw, ""),
        };
        match ty {
            "state" => Self::State,
            "filesystem" => Self::Filesystem(cfg.to_string()),
            other => Self::Unknown(other.to_string()),
        }
    }
}

/// Backend factory: env-driven construction + default fallback.
pub struct BackendFactory;

impl BackendFactory {
    /// Read the `ARF_BACKEND` env var and build a backend.
    ///
    /// - Unset or empty → `BackendSpec::State` → StateBackend
    /// - `state` → StateBackend
    /// - `filesystem:<path>` → FilesystemBackend rooted at `<path>`
    /// - Unknown → `Err(BackendError::Other)`
    pub fn from_env() -> Result<Arc<dyn BackendProtocol>, BackendError> {
        let raw = std::env::var("ARF_BACKEND").unwrap_or_default();
        let spec = if raw.is_empty() {
            BackendSpec::State
        } else {
            BackendSpec::parse(&raw)
        };
        Self::from_spec(spec)
    }

    /// Build a backend from a parsed spec.
    pub fn from_spec(spec: BackendSpec) -> Result<Arc<dyn BackendProtocol>, BackendError> {
        match spec {
            BackendSpec::State => Ok(Arc::new(StateBackend::new())),
            BackendSpec::Filesystem(root) => {
                let backend = FilesystemBackend::new(&root)?;
                Ok(Arc::new(backend))
            }
            BackendSpec::Unknown(ty) => Err(BackendError::Other(format!(
                "unknown backend type: {ty}"
            ))),
        }
    }

    /// Return the safe default backend (StateBackend).
    pub fn default_backend() -> Arc<dyn BackendProtocol> {
        Arc::new(StateBackend::new())
    }
}

/// Resolve a backend from either an instance or a factory callable.
///
/// Mirrors DeepAgents' `_resolve_backend` helper. This is the function
/// that middleware should call when given a generic backend argument.
pub fn resolve_backend<B>(backend: B) -> Arc<dyn BackendProtocol>
where
    B: IntoBackend,
{
    backend.into_backend()
}

/// Trait for "anything that can become a backend" — either an instance
/// or a factory closure.
pub trait IntoBackend {
    fn into_backend(self) -> Arc<dyn BackendProtocol>;
}

impl IntoBackend for Arc<dyn BackendProtocol> {
    fn into_backend(self) -> Arc<dyn BackendProtocol> {
        self
    }
}

impl IntoBackend for Box<dyn BackendProtocol> {
    fn into_backend(self) -> Arc<dyn BackendProtocol> {
        Arc::from(self)
    }
}

impl IntoBackend for StateBackend {
    fn into_backend(self) -> Arc<dyn BackendProtocol> {
        Arc::new(self)
    }
}

impl IntoBackend for FilesystemBackend {
    fn into_backend(self) -> Arc<dyn BackendProtocol> {
        Arc::new(self)
    }
}

/// Factory closure: `Arc<dyn BackendProtocol>` produced on demand.
pub type BackendFactoryFn = Box<dyn Fn() -> Arc<dyn BackendProtocol> + Send + Sync>;

impl IntoBackend for BackendFactoryFn {
    fn into_backend(self) -> Arc<dyn BackendProtocol> {
        (self)()
    }
}
```

### 3.2 `src/lib.rs` 增量

```rust
pub mod factory;

pub use factory::{resolve_backend, BackendFactory, BackendFactoryFn, BackendSpec, IntoBackend};
```

---

## 4. 逐行解释

### 4.1 `BackendSpec` 解析

```rust
let (ty, cfg) = match raw.split_once(':') {
    Some((t, c)) => (t, c),
    None => (raw, ""),
};
```

`state` → no colon → `(raw, "")` → ty="state"
`filesystem:/tmp/foo` → colon → ty="filesystem", cfg="/tmp/foo"

### 4.2 `from_env()` 默认

```rust
let raw = std::env::var("ARF_BACKEND").unwrap_or_default();
let spec = if raw.is_empty() {
    BackendSpec::State
} else {
    BackendSpec::parse(&raw)
};
```

未设置或空 → StateBackend（safe default per Phase 10 G-04）。

### 4.3 `IntoBackend` trait

允许 `resolve_backend()` 接受：
- `Arc<dyn BackendProtocol>` (instance)
- `Box<dyn BackendProtocol>`
- `StateBackend` / `FilesystemBackend` (concrete)
- `BackendFactoryFn` (callable)

消除调用方的 `.into()` 样板。

### 4.4 `BackendFactoryFn`

```rust
pub type BackendFactoryFn = Box<dyn Fn() -> Arc<dyn BackendProtocol> + Send + Sync>;
```

闭包，懒构造 backend。允许"每次访问重建"（如 FilesystemBackend 在不同 cwd 下）。

---

## 5. 测试设计

### 5.1 测试文件：`crates/arf-backend/tests/factory.rs`

| ID | 角度 | 测试内容 | 期望 |
|----|------|---------|------|
| T-F01 | [构造] | `BackendSpec::parse("state")` | `BackendSpec::State` |
| T-F02 | [构造] | `BackendSpec::parse("filesystem:/tmp/foo")` | `BackendSpec::Filesystem("/tmp/foo")` |
| T-F03 | [构造] | `BackendSpec::parse("unknown_type")` | `BackendSpec::Unknown(...)` |
| T-F04 | [构造] | `BackendFactory::from_spec(State)` | StateBackend instance |
| T-F05 | [构造] | `BackendFactory::from_spec(Filesystem("/tmp"))` | FilesystemBackend |
| T-F06 | [构造] | `BackendFactory::from_spec(Unknown)` | `Err(BackendError::Other)` |
| T-F07 | [env] | `from_env()` unset | StateBackend |
| T-F08 | [env] | `from_env()` with `ARF_BACKEND=state` | StateBackend |
| T-F09 | [env] | `from_env()` with `ARF_BACKEND=filesystem:<tempdir>` | FilesystemBackend |
| T-F10 | [env] | `from_env()` with `ARF_BACKEND=bogus` | `Err` |
| T-F11 | [resolve] | `resolve_backend(arc)` | returns same Arc |
| T-F12 | [resolve] | `resolve_backend(StateBackend)` | wraps in Arc |
| T-F13 | [resolve] | `resolve_backend(factory_fn)` | calls factory |
| T-F14 | [default] | `default_backend()` | StateBackend instance |
| T-F15 | [兼容] | App code uses `let b: Arc<dyn BackendProtocol> = BackendFactory::from_env()?;` | compiles |

---

## 6. 与 DeepAgents 的差异

| 维度 | DeepAgents | ARFV1 | 原因 |
|------|------------|-------|------|
| 后端类型 | dict-based factory | `BackendSpec` enum + `from_env` | Rust 强类型 |
| 默认 | StateBackend | StateBackend | 一致 |
| 调用约定 | `ToolRuntime` ctx | `IntoBackend` trait | 适配 async |
| 闭包 | `BackendFactory` type alias | `BackendFactoryFn` Box<dyn Fn> | Rust 闭包语法 |

---

## 7. 不破坏 ARFV1 现有优势

1. **McpNode::local 仍然可用** — Factory 是新抽象，可与现有代码并存
2. **Protocol DI** — `IntoBackend` 保持 trait-based DI
3. **安全默认** — `from_env()` 缺省 → StateBackend（safe）

---

## 8. 实现计划

1. 写 doc（本任务）→ push
2. 批准后：
   - `src/factory.rs`（1 commit）
   - `src/lib.rs`（1 commit）
   - `tests/factory.rs`（1 commit）

预计 3 个原子 commit。

---

## 9. 风险与备选

| 风险 | 影响 | 备选 |
|------|------|------|
| `std::env::var` 测试污染 | 测试间共享环境 | 测试用 `serial_test` crate 或 mock |
| 工厂闭包 Send+Sync 约束 | 不能捕获非 Send 值 | 文档明示 |
| 缺省 StateBackend 误用 | 用户可能想要 FS | env var 强制显式 |

---

## 10. 参考资料

- `docs/v1.x/phase11/11.1-backend-protocol.md`
- `docs/v1.x/phase11/11.2-state-backend.md`
- `docs/v1.x/phase11/11.3-filesystem-backend.md`
- `docs/v1.x/phase11/11.4-composite-backend.md`
- DeepAgents `libs/deepagents/deepagents/backends/protocol.py:982-992`

---

## 版本

- **Phase**: 11
- **Task**: 11.5 (BackendFactory)
- **Doc version**: v1
- **Date**: 2026-07-06
- **下一步**: 实现 + 测试 + commit/push