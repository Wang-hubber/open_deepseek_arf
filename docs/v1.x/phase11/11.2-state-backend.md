# Phase 11 / 11.2 — `StateBackend` 内存后端

> **Task**: 实现 `StateBackend` — 纯内存、线程隔离的 backend 实现，作为 sandbox 默认安全 backend。
> **Gap addressed**: G-01 子项 2, G-04 (Sandbox 后端协议 + 默认安全 Backend)
> **Source**: `docs/v1.x/phase10/99-gap-summary-recommendations.md` §3 G-01/G-04
> **Reference**: DeepAgents `StateBackend` — `libs/deepagents/deepagents/backends/state.py`
> **Status**: 📝 Doc — awaiting user Gitee 精校

---

## 1. Motivation

`StateBackend` 是 DeepAgents 的**默认 backend**（Phase 10/13-§18）—— 当用户没有显式提供 backend 时，`create_deep_agent` 会构造一个内存 `StateBackend`，**禁止 host FS 访问**，强制 sandbox 隔离。

ARFV1 当前**没有 default-secure backend**：
- `McpNode::local` 强制使用 `LocalRuntime`（暴露 host shell）
- 没有"安全默认"选项

**本任务目标**：实现 ARFV1 等价的 `StateBackend`，作为默认 backend，在 Phase 11-M4 与 `SandboxBackendProtocol` 集成。

---

## 2. Scope

### ✅ In scope

1. `StateBackend` 结构 + `BackendProtocol` 实现
2. 内存存储：`HashMap<BackendPath, FileData>` + `DashMap` 并发安全
3. 9 个 trait 方法的完整实现（ls/read/write/edit/delete/glob/grep/upload_files）
4. **可选** `SandboxBackendProtocol` 实现：safe default execute（拒绝所有命令）
5. `StateBackend::new()` + `with_seed(seed: HashMap)` 构造器
6. 完整测试

### ❌ Out of scope

- FilesystemBackend（→ 11.3）
- CompositeBackend（→ 11.4）
- Sandbox shell execution（→ M4 / G-04）
- 持久化（state backend 是会话级，不持久化）

---

## 3. 完整代码

### 3.1 `Cargo.toml` 增量

无新依赖（沿用 11.1）。需添加 `dashmap`：

```toml
[dependencies]
# ... 沿用 11.1
dashmap = "6"
```

### 3.2 `src/state.rs`

```rust
//! `StateBackend` — in-memory, thread-isolated backend (Phase 11 / 11.2).
//!
//! Pure-RAM storage with no host FS or shell access. Safe default for
//! untrusted prompts. Used as the default backend in Phase 11-M4.
//!
//! **Thread safety**: backed by `DashMap` for concurrent reads/writes
//! without blocking the runtime. Distinct from `HashMap + RwLock` which
//! holds a lock across writes.

use std::collections::HashMap;
use std::sync::Arc;

use async_trait::async_trait;
use chrono::Utc;
use dashmap::DashMap;

use crate::error::BackendError;
use crate::path::BackendPath;
use crate::protocol::{BackendProtocol, SandboxBackendProtocol};
use crate::types::{
    DeleteResult, EditResult, ExecuteResponse, FileData, FileInfo, GlobResult, GrepMatch,
    GrepResult, LsResult, ReadResult, WriteResult,
};

/// Default `StateBackend` instance id (used by `id()`).
pub const DEFAULT_STATE_BACKEND_ID: &str = "state-default";

/// In-memory, thread-isolated backend.
///
/// Stores files in a `DashMap<BackendPath, FileData>`. No host FS, no
/// shell. Files persist only for the lifetime of the `StateBackend`
/// instance — typically one per session.
pub struct StateBackend {
    files: Arc<DashMap<BackendPath, FileData>>,
    id: String,
}

impl StateBackend {
    /// Create an empty `StateBackend` with the default id.
    pub fn new() -> Self {
        Self {
            files: Arc::new(DashMap::new()),
            id: DEFAULT_STATE_BACKEND_ID.into(),
        }
    }

    /// Create a `StateBackend` pre-populated with `seed` files.
    ///
    /// Useful for tests and for replaying a previous session's state.
    /// Seed paths MUST be valid `BackendPath`s.
    pub fn with_seed(seed: HashMap<BackendPath, String>) -> Self {
        let now = Utc::now();
        let backend = Self::new();
        for (path, content) in seed {
            let data = FileData {
                content,
                encoding: "utf-8".into(),
                created_at: Some(now),
                modified_at: Some(now),
            };
            backend.files.insert(path, data);
        }
        backend
    }

    /// Create a `StateBackend` with a custom id (for sandbox identity).
    pub fn with_id(id: impl Into<String>) -> Self {
        Self {
            files: Arc::new(DashMap::new()),
            id: id.into(),
        }
    }

    /// Return the number of files currently stored.
    pub fn len(&self) -> usize {
        self.files.len()
    }

    /// Return true if no files are stored.
    pub fn is_empty(&self) -> bool {
        self.files.is_empty()
    }
}

impl Default for StateBackend {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl BackendProtocol for StateBackend {
    async fn ls(&self, path: &BackendPath) -> Result<LsResult, BackendError> {
        let prefix = path.as_str();
        let entries: Vec<FileInfo> = self
            .files
            .iter()
            .filter_map(|kv| {
                let key = kv.key().as_str();
                if key == prefix {
                    return None;
                }
                if !key.starts_with(prefix) {
                    return None;
                }
                // Ensure directory-boundary match: either prefix ends with `/`
                // or next char after prefix is `/`.
                if !prefix.ends_with('/') && key.as_bytes().get(prefix.len()) != Some(&b'/') {
                    return None;
                }
                let data = kv.value();
                Some(FileInfo {
                    path: key.to_string(),
                    is_dir: false,
                    size: data.content.len() as u64,
                    modified_at: data.modified_at,
                })
            })
            .collect();
        Ok(LsResult {
            error: None,
            entries: Some(entries),
        })
    }

    async fn read(
        &self,
        path: &BackendPath,
        offset: u32,
        limit: u32,
    ) -> Result<ReadResult, BackendError> {
        let Some(data) = self.files.get(path).map(|r| r.value().clone()) else {
            return Ok(ReadResult {
                error: Some("file_not_found".into()),
                file_data: None,
            });
        };
        // Apply offset/limit on lines.
        let lines: Vec<&str> = data.content.lines().collect();
        let start = (offset as usize).min(lines.len());
        let end = if limit == 0 {
            lines.len()
        } else {
            (start + limit as usize).min(lines.len())
        };
        let slice: String = lines[start..end].join("\n");
        Ok(ReadResult {
            error: None,
            file_data: Some(FileData {
                content: slice,
                encoding: data.encoding,
                created_at: data.created_at,
                modified_at: data.modified_at,
            }),
        })
    }

    async fn write(&self, path: &BackendPath, content: &str) -> Result<WriteResult, BackendError> {
        let now = Utc::now();
        let data = FileData {
            content: content.to_string(),
            encoding: "utf-8".into(),
            created_at: self
                .files
                .get(path)
                .map(|r| r.value().created_at.unwrap_or(now)),
            modified_at: Some(now),
        };
        self.files.insert(path.clone(), data);
        Ok(WriteResult {
            error: None,
            path: Some(path.to_string()),
        })
    }

    async fn edit(
        &self,
        path: &BackendPath,
        old_string: &str,
        new_string: &str,
        replace_all: bool,
    ) -> Result<EditResult, BackendError> {
        let Some(mut kv) = self.files.get_mut(path) else {
            return Err(BackendError::FileNotFound {
                path: path.to_string(),
                code: "file_not_found",
            });
        };
        let data = kv.value_mut();
        let occurrences = data.content.matches(old_string).count();
        if occurrences == 0 {
            return Err(BackendError::EditMatch {
                reason: format!("old_string not found in {path}"),
            });
        }
        if !replace_all && occurrences > 1 {
            return Err(BackendError::EditMatch {
                reason: format!(
                    "old_string matched {occurrences} times in {path} (not unique; pass replace_all=true)"
                ),
            });
        }
        let new_content = if replace_all {
            data.content.replace(old_string, new_string)
        } else {
            data.content.replacen(old_string, new_string, 1)
        };
        let actual_replacements = if replace_all {
            occurrences as u32
        } else {
            1
        };
        data.content = new_content;
        data.modified_at = Some(Utc::now());
        Ok(EditResult {
            error: None,
            path: Some(path.to_string()),
            occurrences: actual_replacements,
        })
    }

    async fn delete(&self, path: &BackendPath) -> Result<DeleteResult, BackendError> {
        // Recursive delete: remove exact match + any key sharing `path + "/"` prefix.
        let prefix = path.as_str();
        let mut to_remove: Vec<BackendPath> = Vec::new();
        for kv in self.files.iter() {
            let k = kv.key().as_str();
            if k == prefix || k.starts_with(&format!("{prefix}/")) {
                to_remove.push(kv.key().clone());
            }
        }
        if to_remove.is_empty() {
            return Err(BackendError::FileNotFound {
                path: path.to_string(),
                code: "file_not_found",
            });
        }
        for key in to_remove {
            self.files.remove(&key);
        }
        Ok(DeleteResult {
            error: None,
            path: Some(path.to_string()),
        })
    }

    async fn glob(
        &self,
        pattern: &str,
        _base: Option<&BackendPath>,
    ) -> Result<GlobResult, BackendError> {
        // Minimal glob: match using a simple `*` and `**` interpreter.
        // Production will use `globset` in FilesystemBackend (11.3).
        let matches: Vec<FileInfo> = self
            .files
            .iter()
            .filter(|kv| glob_match_simple(pattern, kv.key().as_str()))
            .map(|kv| {
                let data = kv.value();
                FileInfo {
                    path: kv.key().to_string(),
                    is_dir: false,
                    size: data.content.len() as u64,
                    modified_at: data.modified_at,
                }
            })
            .collect();
        Ok(GlobResult {
            error: None,
            matches: Some(matches),
            truncated: false,
        })
    }

    async fn grep(
        &self,
        pattern: &str,
        path: Option<&BackendPath>,
        glob_filter: Option<&str>,
    ) -> Result<GrepResult, BackendError> {
        let path_prefix = path.map(|p| p.as_str().to_string()).unwrap_or_default();
        let mut matches: Vec<GrepMatch> = Vec::new();
        for kv in self.files.iter() {
            let p = kv.key().as_str();
            if !path_prefix.is_empty() && !p.starts_with(&path_prefix) {
                continue;
            }
            if let Some(filter) = glob_filter {
                if !glob_match_simple(filter, p) {
                    continue;
                }
            }
            for (idx, line) in kv.value().content.lines().enumerate() {
                if line.contains(pattern) {
                    matches.push(GrepMatch {
                        path: p.to_string(),
                        line: (idx + 1) as u32,
                        text: line.to_string(),
                    });
                }
            }
        }
        Ok(GrepResult {
            error: None,
            matches: Some(matches),
            truncated: false,
        })
    }

    async fn upload_files(
        &self,
        files: Vec<(BackendPath, Vec<u8>)>,
    ) -> Result<Vec<Result<(), BackendError>>, BackendError> {
        let mut results = Vec::with_capacity(files.len());
        for (path, bytes) in files {
            let content = match String::from_utf8(bytes.clone()) {
                Ok(s) => s,
                Err(_) => {
                    results.push(Err(BackendError::Io(format!(
                        "non-UTF8 bytes at {path} (base64 encoding not yet supported)"
                    ))));
                    continue;
                }
            };
            self.write(&path, &content).await?;
            results.push(Ok(()));
        }
        Ok(results)
    }
}

#[async_trait]
impl SandboxBackendProtocol for StateBackend {
    fn id(&self) -> &str {
        &self.id
    }

    async fn execute(
        &self,
        command: &str,
        _timeout_ms: Option<u64>,
    ) -> Result<ExecuteResponse, BackendError> {
        // StateBackend is the SAFE default — reject all shell execution.
        // FilesystemBackend / LocalShellBackend (11.3 / M4) override this.
        Err(BackendError::Unsupported("execute"))
            .map_err(|e| {
                // Wrap with command context for debugging.
                tracing::debug!(command, error = %e, "StateBackend::execute rejected");
                e
            })
            .map(|_: ExecuteResponse| unreachable!())
    }
}

// ---------------------------------------------------------------------------
// Internal: minimal glob matcher (sufficient for StateBackend tests).
// Production will use `globset` crate in FilesystemBackend (11.3).
// ---------------------------------------------------------------------------

fn glob_match_simple(pattern: &str, path: &str) -> bool {
    fn char_match(pat: &[u8], path: &[u8]) -> bool {
        let rest_pat = |i: usize| pat.get(i..).unwrap_or(&[]);
        let rest_path = |i: usize| path.get(i..).unwrap_or(&[]);
        match (pat.first(), path.first()) {
            (None, None) => true,
            (None, Some(_)) => false,
            (Some(b'*'), _) => {
                if pat.get(1) == Some(&b'*') {
                    if path.is_empty() {
                        char_match(rest_pat(2), path)
                    } else {
                        char_match(rest_pat(2), path) || char_match(pat, rest_path(1))
                    }
                } else if path.is_empty() || path[0] == b'/' {
                    char_match(rest_pat(1), path)
                } else {
                    char_match(rest_pat(1), path) || char_match(pat, rest_path(1))
                }
            }
            (Some(b'?'), Some(_)) => char_match(rest_pat(1), rest_path(1)),
            (Some(a), Some(b)) if a == b => char_match(rest_pat(1), rest_path(1)),
            (Some(_), None) => false,
            (Some(_), Some(_)) => false,
        }
    }
    char_match(pattern.as_bytes(), path.as_bytes())
}
```

> **设计取舍**：
> - `DashMap` 而非 `RwLock<HashMap>`：避免读写互斥；FilesystemBackend 11.3 仍可能用 `RwLock`（FS 操作本就慢）
> - `read(offset, limit)` 按**行**分割（与 DeepAgents 一致）
> - `delete` 是**递归**的：删 `path` 加所有 `path/...` 子键
> - `execute` 拒绝所有命令 → 与 Phase 10 G-04 "safe default" 目标一致

### 3.3 `src/lib.rs` 增量

```rust
pub mod state;

pub use state::{StateBackend, DEFAULT_STATE_BACKEND_ID};
```

---

## 4. 逐行解释

### 4.1 结构

- `files: Arc<DashMap<...>>`：`Arc` 让 `StateBackend` 可以 clone 共享（与 `Arc<dyn BackendProtocol>` 协同）；`DashMap` 提供 lock-free 读
- `id: String`：sandbox 身份（M4 多 sandbox 时区分）

### 4.2 `ls` 目录边界判断

```rust
if !prefix.ends_with('/') && key.as_bytes().get(prefix.len()) != Some(&b'/') {
    return None;
}
```

这避免 `/foo` 匹配 `/foobar`。与 `BackendPath::is_ancestor_of` 的语义一致（11.1 path.rs:84-91）。

### 4.3 `read(offset, limit)` 行切片

```rust
let lines: Vec<&str> = data.content.lines().collect();
let start = (offset as usize).min(lines.len());
let end = if limit == 0 {
    lines.len()
} else {
    (start + limit as usize).min(lines.len())
};
```

- `offset = 0, limit = 0` → 全部内容
- `offset = 5, limit = 10` → 第 6-15 行
- 越界不 panic（`.min()` 保证）

### 4.4 `edit` 唯一性检查

复用 11.1 的 `BackendError::EditMatch` 语义：
- 0 匹配 → `EditMatch { reason: "not found" }`
- N>1 匹配 + `replace_all=false` → `EditMatch { reason: "not unique" }`

### 4.5 `delete` 递归

DeepAgents 语义：删除 `path` + 所有 `path + "/" + ...` 子键。FilesystemBackend 11.3 会对真实目录做 `tokio::fs::remove_dir_all`。

### 4.6 `execute` 拒绝

StateBackend 是 **safe default**——拒绝所有 shell 命令。这是 Phase 10 G-04 的核心：app 显式 opt-in 到 `LocalShellBackend` 才会有 host shell。

---

## 5. 测试设计

### 5.1 测试文件：`crates/arf-backend/tests/state_backend.rs`

| ID | 角度 | 测试内容 | 期望 |
|----|------|---------|------|
| T-S01 | [构造] | `StateBackend::new()` 创建空 backend | `len() == 0` |
| T-S02 | [构造] | `StateBackend::with_seed({...})` 预填充 | `len() == seed.len()` |
| T-S03 | [构造] | `StateBackend::with_id("custom")` | `id() == "custom"` |
| T-S04 | [方法] | write → read 往返 | 内容一致 |
| T-S05 | [方法] | read 不存在的文件 | `error: "file_not_found"` |
| T-S06 | [方法] | edit 唯一匹配 | `occurrences == 1`，内容替换 |
| T-S07 | [方法] | edit 多次匹配 + replace_all=false | `Err(EditMatch)` |
| T-S08 | [方法] | edit 多次匹配 + replace_all=true | `occurrences == N` |
| T-S09 | [方法] | edit 文件不存在 | `Err(FileNotFound)` |
| T-S10 | [方法] | delete 单个文件 | 文件从 map 消失 |
| T-S11 | [方法] | delete 目录路径 | 删所有子文件 |
| T-S12 | [方法] | delete 不存在的路径 | `Err(FileNotFound)` |
| T-S13 | [方法] | ls 返回所有子文件 | entries.len() 正确 |
| T-S14 | [方法] | ls 不返回父目录边界外的文件 | `/foo` 不匹配 `/foobar` |
| T-S15 | [方法] | glob `**/*.txt` | 只匹配 .txt 文件 |
| T-S16 | [方法] | grep pattern + glob filter | 只匹配过滤后的文件 |
| T-S17 | [方法] | read with offset=2, limit=5 | 切片正确 |
| T-S18 | [方法] | read with offset=u32::MAX | 不 panic，返回空 |
| T-S19 | [方法] | upload_files batch | 全部写入成功 |
| T-S20 | [方法] | upload_files non-UTF8 | 对应项返回错误 |
| T-S21 | [trait] | `Box<dyn BackendProtocol>` 可装 StateBackend | compile OK |
| T-S22 | [trait] | `Arc<dyn BackendProtocol>` 多线程共享 | 并发写无丢失 |
| T-S23 | [sandbox] | `id()` 返回 DEFAULT_STATE_BACKEND_ID | `"state-default"` |
| T-S24 | [sandbox] | `execute("any")` 拒绝 | `Err(Unsupported)` |
| T-S25 | [并发] | 100 个并发 write | 全部成功 |
| T-S26 | [并发] | write + read 并发 | 最终一致 |
| T-S27 | [边界] | write 超大内容 (1MB) | 不 panic |
| T-S28 | [时间] | write 后 modified_at 更新 | 与 write 时间近似 |

---

## 6. 与 DeepAgents StateBackend 的差异

| 维度 | DeepAgents | ARFV1 | 原因 |
|------|------------|-------|------|
| 存储 | 字典（langgraph state） | `DashMap<BackendPath, FileData>` | Rust 类型安全 + 并发 |
| 编码 | v1 (list[str]) / v2 (str) | 仅 v2 (str + encoding 标记) | 跳过 v1 兼容 |
| `execute` | 不实现（StateBackend 是 file-only） | 拒绝所有命令（`Unsupported`） | ARF 安全默认 |
| `id` | 无 | `id: String` | 与 M4 sandbox 协同 |

---

## 7. 不破坏 ARFV1 现有优势

1. **Bus 多 runtime** — StateBackend 不破坏；它只是其中一个 backend 实现
2. **Protocol DI** — `StateBackend: BackendProtocol + SandboxBackendProtocol`，与现有 trait 风格一致
3. **异步优先** — 所有方法 async（`upload_files` 在并发场景下顺序 await，未来可改 `futures::join_all`）

---

## 8. 实现计划

按 CLAUDE.md + 11.1 节奏：

1. 写 doc（本任务）→ push → 用户精校
2. 批准后实现：
   - `Cargo.toml` 加 `dashmap`（1 commit，与 11.1 同一 crate）
   - `src/state.rs`（1 commit）
   - `src/lib.rs` 添加 `pub mod state;` + re-export（1 commit）
   - `tests/state_backend.rs`（1 commit）

预计 3 个原子 commit。

---

## 9. 风险与备选

| 风险 | 影响 | 备选 |
|------|------|------|
| `DashMap` 内存增长 | 长时间 session 可能 OOM | 后续 task 加容量限制 + 驱逐 |
| 全部 inline glob/grep | 性能 vs `globset`/`regex` | 大数据量场景建议 FilesystemBackend (11.3) |
| `upload_files` 不支持 base64 | 二进制上传限制 | 加 `encoding` 字段解析（M4 后续） |

---

## 10. 参考资料

- `docs/v1.x/phase11/11.1-backend-protocol.md` — trait 定义
- `docs/v1.x/phase10/13-sandbox-shell.md` §18 — default backend 设计
- `docs/v1.x/phase10/99-gap-summary-recommendations.md` G-04 — sandbox 安全默认
- DeepAgents `libs/deepagents/deepagents/backends/state.py` — 参考实现

---

## 版本

- **Phase**: 11
- **Task**: 11.2 (StateBackend)
- **Doc version**: v1
- **Date**: 2026-07-06
- **下一步**: 实现 + 测试 + commit/push